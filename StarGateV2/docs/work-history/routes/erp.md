# /erp

## 2026-06-29 · UI 개편 · 커맨드 센터

- 캐릭터, 다음 작전과 액션 큐를 첫 화면 우선순위에 맞춰 재배치했다.
- 상단 HUD와 지표를 큰 평면 타일·pill 액션 기반으로 정리하고 다크·골드 톤을 유지했다.
- 검증: 대시보드 page와 CSS 변경을 연결 커밋별로 대조했다.
- 관련 커밋: `bbea47a`, `f7fc389`, `4001666`, `dfb4432`

## 2026-07-01 · 지표 정확성 개선

- HP·SAN 막대를 현재값/최대값 비율과 구간 눈금으로 표시하도록 수정했다.
- 진행 세션 지표를 KST 기준 당일 세션으로 계산하고 신호 그리드를 재구성했다.
- 검증: 공용 Bar 계산과 대시보드 지표 산식을 현재 구현에서 확인했다.
- 관련 커밋: `f8a120c`, `cfb21c4`

## 2026-07-30 · 기능 변경 · 실시간 운용 대시보드

- 대시보드 서버 집계와 API가 같은 조회 서비스를 사용하고 전체 화면을 `initialData` 기반 Query로 전환했다.
- 알림·잔액·세션·위키·캐릭터·가입/작전 지표가 관련 resource 변경 후 갱신되며 장애 중에는 60초 polling으로 폴백한다.
- 검증: realtime Query 계약 테스트, `pnpm lint`, `pnpm typecheck`, `pnpm build:web`
- 관련 커밋: `bba8924`

## 2026-07-31 · 성능 최적화 · 대시보드 집계 경량화

- "오늘 세션" 카운트가 한 달치 세션과 참여자 enrich(약 5쿼리)를 로드하던 것을 enrich 생략 카운트 함수로 교체했다 (60초 폴링마다 절약).
- 최근 위키 3건을 전체 로드 + JS 재정렬 대신 DB sort+limit 조회로 교체했다.
- 검증: KST 경계·구경로 등가성 oracle 테스트 7건, `pnpm build`
- 관련 커밋: `e1bd14f`

## 2026-07-31 · 성능 최적화 · 대시보드 304 재검증

- 대시보드 GET에 ETag/304 도입 — 60초 폴링에서 데이터 불변 시 응답 바디 0B. 페이로드 소스 배열(캐릭터/위키/세션) 정렬에 보조 키를 추가해 해시 안정화.
- 검증: http-cache 단위 테스트, `pnpm build`
- 관련 커밋: `0390f80`

## 2026-08-07 · 보안 수정 · 최근 위키 공개 범위 고정

- 대시보드의 최근 위키 조회가 공유 조회 함수의 기본값에 의존하지 않고 비공개 문서를 명시적으로 제외하도록 고정했다.
- 검증: 구현 커밋 diff와 현재 대시보드의 인증·게스트 조회 경로에서 `includePrivate: false` 적용을 확인했다.
- 관련 커밋: `da285edb`

## 2026-08-07 · 기능 변경 · GM 표시 신원과 운용 캐릭터 분리

- GM 계정이 소유한 단일 NPC를 ERP 표시 신원으로 사용할 수 있게 해 헤더와 대시보드에는 수잔 델라웨어의 이름·H 등급·CLAIRVOYANCE 정보가 노출되도록 했다.
- 표시 신원과 경제 처리 대상을 분리해 기존 GM `MAIN` 캐릭터가 크레딧·인벤토리 운용 주체로 계속 사용되며, 대시보드 잔액 `¤ 1,000`도 유지된다.
- 검증: 관련 앱·공용 DB 테스트, `pnpm typecheck`, `pnpm lint`, `pnpm build`, Credentials 로그인 후 `/erp`의 수잔 H 표시·CLAIRVOYANCE SD 이미지·잔액·가로 넘침 없음, `/erp/admin`의 GM 전용 메뉴 접근, 브라우저 콘솔 오류 0건
- 관련 커밋: `2f19fd84`, `2ff49207`

## 2026-08-10 · 기능 추가 · 게스트 읽기 전용 미리보기

- 로그인 화면에서 자격증명 없이 `U` 등급 게스트 세션으로 ERP에 들어가 공개 가능한 대시보드·인물·위키·카탈로그를 둘러볼 수 있게 했다.
- 게스트의 모든 데이터 변경 요청과 상태를 바꾸는 GET을 공통 경계에서 차단하고, 개인 소유 데이터는 조회하지 않으며 ERP API 캐시를 쿠키별로 분리했다.
- 전체 ERP에 게스트 미리보기 배너를 표시하고 계정·경제·인벤토리·거래·알림 같은 개인 영역은 빈 상태 또는 안전한 미리보기로 제공한다.
- 검증: 게스트 접근 계약 테스트 11/11, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 실제 Auth.js 게스트 로그인 API 통합 확인, 데스크톱·390×844 브라우저 확인(가로 넘침·콘솔 오류 없음), critical risk review
- 관련 커밋: `aa3ce2d8`

## 2026-08-10 · 보안 개선 · 게스트 공개 데이터 경계

- 계정 회원 전용 콘텐츠와 게스트 공개 미리보기를 분리하고, 공개 API·RSC 응답은 화면에 필요한 필드만 직렬화하도록 강화했다.
- 작전 보고서·운영 식별자·소유자 메타·개인 연구 원장이 게스트 응답에 섞이지 않도록 경계를 고정했다.
- 검증: 게스트 공개 경계 집중 테스트 24건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 실제 Auth.js 게스트 API/RSC 통합, 데스크톱·390×844 브라우저 확인, critical risk review
- 관련 커밋: `2df03010`

## 2026-08-13 · 성능 최적화 · 초기 hydration과 LCP

- 공용 날짜·시각 포맷을 KST로 고정해 Vercel UTC SSR과 한국 브라우저 사이의 알림 시각 불일치 및 대시보드 전체 client 재생성을 제거했다.
- 첫 화면 캐릭터 hero 이미지를 eager 로드해 실제 LCP 후보가 lazy 로드되던 경고를 해소했다.
- 검증: 운영 JTEST 직접 진입 1.85~2.20초 기준선 측정, UTC 개발 서버에서 `03:59 → 12:59` hydration mismatch 재현 후 새 SSR 탭 콘솔 오류·경고 0건, 신규 계약 5건 + 기존 대시보드 계약 2건, `pnpm typecheck`, `pnpm lint`, `pnpm build`
- 관련 커밋: `9a80b2f2`, `4d8c01dd`

## 2026-08-14 · 기능 추가 · 주식 알림 표시

- 대시보드 최근 알림에서 목표가·회차 등락·공시·시즌 순위를 구분하는 새 `STOCK` 알림 유형을 기존 개인 알림과 같은 방식으로 표시한다.
- 검증: 알림 타입 계약, `pnpm typecheck`, 전체 `pnpm lint`, production build, 인증 로컬 대시보드 확인
- 관련 커밋: `fb012220`
- 운영 경계: 라이브 개인 알림은 생성하지 않았다.

## 2026-08-30 · 성능 최적화 · 표시 캐릭터와 알림 조회 축소

- 대시보드가 쓰는 이름·코드네임·등급·초상·HP/SAN/포인트만 DB projection으로 읽고, 캐릭터 시트·로어 전문을 RSC/API에 직렬화하지 않는다. GM의 표시 NPC와 크레딧을 조회하는 경제 MAIN은 계속 별도로 선택하며 중복·소유권·ACTIVE GM fallback 정책은 유지한다.
- 최근 알림은 기존처럼 최대 5건을 표시하되 DB에서 20건을 읽고 자르는 대신 처음부터 5건만 조회한다.
- 실제 소유자 7명·캐릭터 8개의 main/display/byId 22회 비교가 모두 일치했다. 캐릭터 부분 JSON 평균은 9,009.68→230.32B(-97.44%), BSON 평균은 9,279.45→226.82B였다. 전체 대시보드 payload 절감률로 해석하지 않는다.
- 알림 결과도 7/7 동일했고 DB 조회 행은 110→34, BSON 합계는 30,167→9,069B로 줄었다. 브라우저로 보내는 알림 5건은 변경하지 않았다.
- 검증: GM 표시·MAIN·legacy·중복·비활성·소유권 mock 9건, 대시보드 계약 6건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증 GM 1280px·390px 신원·초상·가로 넘침 확인, critical risk review P0–P2 없음
- 관련 구현 커밋: `45d9fc0a`
- 운영 경계: 계정·캐릭터·크레딧·인벤토리·알림을 쓰지 않았으며 다른 작업의 아이콘·장비 변경은 커밋에 포함하지 않았다.

## 2026-09-08 · UI 개선 · 대시보드 정보 배치와 반응형

- 프로필과 다음 작전을 데스크톱 2열로 배치하고 처리할 일을 별도 전체폭 영역으로 분리했다. 기존 최대 4개 표시 제한을 없애 처리 항목 수와 실제 목록이 일치하며, 1개 항목은 전체폭으로 읽을 수 있다.
- 지표 8개는 기본 4열·2행으로 정리하고 작은 화면에서는 크레딧과 포인트에 전체 행을 배정했다. 긴 요원명·코드명·처리 항목 상세는 줄바꿈되며 작은 초상과 버튼 라운드로 모바일 프로필을 정리했다.
- 이중선·바코드·반복 배경 장식을 줄이고 한글 제목과 보조 라벨, 빈 상태 높이와 작업 목록 구분선을 정리했다. 데이터 조회·계산·상태 분기·링크 목적지는 유지했다.
- 검증: 대시보드 기존 계약 테스트 6건, `pnpm typecheck`, `pnpm lint`, 독립 읽기 전용 리뷰를 통과했다. 인증 로컬 GM의 1920·1440·1280·1279·834·390·320px 및 플레이어·게스트의 1440·390·320px에서 API 값과 지표 8개·링크·초상 일치와 가로 넘침 없음을 확인했다.
- 로컬 GET 응답을 가로채는 브라우저 fixture로 긴 이름·제목, 큰 잔액/포인트, HP/SAN, 여러 작전·알림 유형, 0값·캐릭터 없음·미연동·정합성 경고 상태를 확인했다. 브라우저 오류와 업무 mutation은 0건이었다.
- 관련 구현 커밋: `6452327e`
- 운영 경계: 대시보드 클라이언트와 CSS 2파일만 변경했다. 헤더·사이드바·하위 페이지·DB·인증·권한·Query와 라이브 데이터는 변경하지 않았으며 push·배포는 하지 않았다. 이 구현 커밋만 독립적으로 revert할 수 있다.

## 2026-09-08 · UI 개선 · 신원 카드와 작전실 시각 개편

- 직전 정보 배치 개선이 밋밋하다는 사용자 피드백에 따라 대시보드를 재구성했다. NOVUS 문장과 큰 요원명, 밝은 초상 영역으로 신원 카드를 강조하고 기존 세계관 지도를 다음 작전 브리핑 배경으로 활용했다. 예정 작전이 없을 때도 STANDBY와 세션 달력 진입을 표시한다.
- 처리할 일과 운용 지표를 데스크톱에서 나란히 배치하고 크레딧·포인트를 크게 표시했다. 나머지 지표 6개와 하단 작전·응답·알림·최근 변경은 밀도 있는 행으로 정리했다. 모바일에서는 카드를 순서대로 배치하고 큰 금액에 전체 행을 할당한다.
- 대시보드의 픽셀 글꼴 opt-in을 제거해 보조 라벨과 수치의 일반 고정폭 글꼴을 복구했다. 캐릭터 픽셀 이미지, 기존 아이콘, 데이터 계산·8개 지표의 값과 링크·상태 분기를 유지했다.
- 검증: 기존 대시보드 계약 테스트 6건, `pnpm typecheck`, `pnpm lint`, 독립 읽기 전용 리뷰를 통과했다. 인증 로컬 GM의 1920·1440·1280·1279·834·390·320px 및 플레이어·게스트의 1440·390·320px에서 API 값·링크·초상 일치와 가로 넘침 없음을 확인했다. 데스크톱과 모바일 실제 화면을 직접 검토했다.
- 로컬 GET fixture로 긴 이름·코드명·작전 제목, 큰 크레딧/포인트, HP/SAN, 여러 작전·알림, 0값·캐릭터 없음·미연동·정합성 경고 상태를 확인했다. 마지막 모바일 지도 정렬 보완 후 390·834px을 재확인했다. 브라우저 오류와 업무 mutation은 0건이었다.
- 관련 구현 커밋: `7f95bfdc`
- 운영 경계: 대시보드 클라이언트와 CSS만 수정하고 기존 정적 자산을 재사용했다. push·배포는 하지 않았으며 이 구현 커밋을 독립적으로 revert할 수 있다.

## 2026-09-08 · UI 개선 · 신원과 운용 정보의 시각 흐름 통합

- 개별 카드들이 비슷한 비중으로 보여 아쉽다는 후속 사용자 피드백에 따라, 전체폭 요원 정보와 8개 지표를 하나의 영역으로 통합했다. 반복 제목·문장 헤더와 초상 액자를 제거하고 요원명과 실제 캐릭터 초상이 먼저 보이도록 배치했다.
- 다음 작전과 처리할 일을 연결된 영역으로 묶었다. 프로필 진입은 보조 버튼으로, 작전·세션 달력 진입은 주요 버튼으로 구분하고, 작전이 없을 때 다음 행동을 안내한다. 데이터 계산·Query·권한·지표의 값과 링크는 유지했다.
- 모바일에서는 초상과 요원 정보를 나란히 읽고 HP/SAN·버튼을 그 아래에 배치한다. 재무 지표 2행과 보조 지표 3열·2행으로 정리했으며, 좁은 데스크톱의 긴 가입일 수치와 이미지 없는 프로필의 원형 이니셜 표시를 보완했다.
- 검증: 기존 대시보드 계약 테스트 6건, `pnpm typecheck`, `pnpm lint`, 독립 읽기 전용 리뷰를 통과했다. 인증 GM 1920·1440·1280·1279·834·390·320px, 플레이어·게스트 1440·390·320px에서 API 값·링크·이미지와 화면 넘침을 확인했다.
- 로컬 GET fixture로 긴 이름·작전 제목, 큰 금액·포인트·가입일, HP/SAN, 여러 작전·알림, 빈 상태·미연동·정합성 경고를 확인했다. 이미지 없는 실플레이어는 마지막 보완 후 1440·390·320px에서 재검증했다. 브라우저 오류와 업무 mutation은 0건이었다.
- 관련 구현 커밋: `d125ef51`
- 운영 경계: 대시보드 클라이언트와 CSS만 변경했다. push·배포는 하지 않았으며 이번 구현을 독립적으로 revert할 수 있다.

## 2026-09-08 · UI 개선 · 팔레트와 실제 프로필 정보

- 사용자가 수용한 배치는 유지하고 녹색 기운을 제거해 ERP 공용 먹색·웜그레이·골드로 색상과 지도 배경을 통일했다.
- 운용 지표는 크레딧·잔여 포인트·확인할 응답·미확인 알림 4개로 정리했다. 캐릭터 수·가입일·누적 작전·금일 세션은 상단에서 제외하며 기존 API 계약은 유지한다. 응답 수는 아래 표시된 작전 기준이며 크레딧은 경제 MAIN의 잔액임을 명시했다.
- 등록된 직책·부서·AGENT 직군·한 마디로 프로필을 채우고 초상 아래 코드네임을 배치했다. 비어 있거나 CLASSIFIED인 값은 숨기고 상세 원문은 전송하지 않는다.
- 캐릭터 projection에 필요한 scalar 4개만 추가하고 fallback 재조회에 소유자 조건을 함께 적용했다. 캐릭터 생성·편집·삭제 성공 시 대시보드 캐시를 무효화해 재진입 후 최신 프로필을 읽는다.
- 검증: shared-db 빌드, 소유권·projection mock 10건, 대시보드 계약 6건, `pnpm typecheck`, `pnpm lint`, critical risk review를 통과했다. 인증 GM 1920·1440·1280·1279·834·390·320px, 플레이어·게스트 1440·390·320px에서 실제 값·링크·이미지와 넘침을 확인했다. 긴 프로필·큰 수치·CLASSIFIED·legacy·빈 상태·경고 fixture도 통과했다.
- 인증 브라우저에서 Dossier PATCH를 로컬 응답으로 가로채 저장한 뒤 같은 문서 내 대시보드 재진입 시 캐시가 갱신됨을 60초 polling 이전에 확인했다. 브라우저 오류와 라이브 업무 mutation은 0건이었다.
- 관련 구현 커밋: `032503b7`
- 운영 경계: 라이브 캐릭터·크레딧·인벤토리·알림은 변경하지 않았다. push·배포 없이 로컬 커밋했으며 이번 구현을 독립적으로 revert할 수 있다.

## 2026-09-08 · UI 개선 · 프로필 중앙 여백

- 프로필 가운데가 비어 보인다는 후속 사용자 피드백에 따라, 데스크톱에서 이름·버튼과 직책·대사·상태를 분리해 초상까지 3열로 읽도록 배치했다. 새로운 데이터나 장식을 추가하지 않고 기존 요약을 중앙에 배치했다.
- 초상을 확대하고 코드네임과의 간격을 고정했다. 요약이 없는 프로필은 전체 폭을 제한하며 태블릿과 모바일에서는 기존 세로 순서로 전환한다. 팔레트·운용 지표·데이터·권한·캐시 동작은 유지한다.
- 검증: 기존 대시보드 계약 6건, `pnpm typecheck`, `pnpm lint`, 최종 diff 리뷰를 통과했다. 인증 GM 1920·1440·1280·1279·834·390·320px, 플레이어·게스트 1440·390·320px에서 실제 값·링크·이미지와 넘침을 확인했다. 긴 이름·대사·큰 수치·AGENT 상태·CLASSIFIED·legacy·빈 상태·경고 fixture도 통과했고 브라우저 오류와 업무 mutation은 0건이었다.
- 관련 구현 커밋: `0f7f66b1`
- 운영 경계: 대시보드 클라이언트와 CSS 2파일만 수정했다. push·배포 없이 로컬 커밋했으며 이번 배치 변경만 독립적으로 revert할 수 있다.

## 2026-09-08 · UI 개선 · 프로필 격자 배경

- 사용자가 제시한 https://chatgpt.com/cyber/ 상단을 확인하고, 기존 먹색·골드 프로필에 20px 격자와 초상 쪽을 강조하는 페이드를 적용했다. 모바일은 18px 격자와 낮은 대비로 조정했다.
- CSS 배경 레이어만 추가해 3열 정보 배치·초상·운용 지표와 데이터 흐름을 유지하며, 격자는 포인터 입력을 받지 않는다.
- 검증: `pnpm typecheck`, `pnpm lint`, 최종 diff 리뷰. 인증 GM 1920·1440·1280·1279·834·390·320px와 플레이어·게스트 1440·390·320px의 실제 값·링크·초상·가로 넘침 및 긴 문구·빈 상태 fixture를 확인했다. 브라우저 오류와 업무 mutation은 0건이었다. 1440·390px에서 실제 격자·마스크 렌더와 프로필 버튼 hit test를 추가 확인했다.
- 관련 구현 커밋: `69a72c1f`
- 운영 경계: 대시보드 CSS 1파일만 변경했다. 새 이미지·스크립트·DB 변경과 push·배포는 없다.
