# `/erp/hall-of-fame` 작업 이력

## 2026-08-22 · 기능 개발 · 팀 연구 누적 공로 시상대

- 팀 연구 모금·가속에 실제 사용한 CR을 전체 기간 합산해 누적 공로 TOP 3를 표시한다. 데스크톱은 1위를 강조한 시상대, 모바일은 1→2→3 세로 카드로 구성했다.
- 비로그인은 차단하면서 회원과 ERP 게스트에는 같은 공개 TOP 3를 제공한다. 공개 API는 순위·코드네임·누적 CR·기여 횟수만 반환하며 캐릭터·사용자·요청 식별자와 개별 원장 시각은 제외한다.
- 첫 일일 실행 전에는 원장을 읽기 전용으로 집계하고, 이후에는 매일 21:00 KST 스냅샷을 RSC 초기 데이터와 TanStack Query에 연결한다. realtime 장애와 게스트 환경에서는 5분 polling으로 갱신한다.
- 검증: 웹 집중 테스트 22건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 비로그인 API 401, 회원·게스트 동일 TOP 3와 공개 필드 확인, 1440×1000·390×844에서 콘솔 오류·네트워크 실패·가로 넘침 없음, critical risk review
- 관련 커밋: `9b2b7897`
- 운영 경계: 라이브 연구·크레딧·인벤토리·Discord·Dokploy 상태는 변경하지 않았다.

## 2026-08-22 · 안정성 개선 · 마지막 일일 스냅샷 보존

- 5분 polling 또는 realtime 재검증이 실패해도 마지막으로 성공한 TOP 3와 기준 시각을 유지하고, 최신 상태를 확인하지 못했다는 경고와 수동 재시도만 표시하도록 보강했다.
- 최초 조회처럼 사용할 데이터가 전혀 없을 때만 전체 오류 상태를 보여 주며, 빈 순위 스냅샷도 같은 stale-data 경고 계약을 따른다.
- 검증: Hall of Fame·TanStack Query·관리자 집중 테스트 17건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 1440×1000·390×844 및 API 500 강제 상황에서 순위 유지·가로 넘침·콘솔 상태 확인, critical risk review
- 관련 커밋: `a9037103`
- 운영 경계: 브라우저 QA는 조회와 로컬 API 실패 주입만 수행했으며 라이브 DB·Discord·Dokploy 상태는 변경하지 않았다.

## 2026-08-22 · UI 개선 · 기관 수상 기록판 개편

- 동일한 크기의 TOP 3 카드 대신 캐릭터 초상을 활용한 기관 수상 기록판으로 재구성했다. 1위는 대형 전신 패널, 2·3위는 압축 패널로 구분하고 순위 표기 중복을 제거했다.
- 기록판 폭을 1280px로 제한하고 container query로 데스크톱 2열, 중간 폭 1위+2열, 모바일 1→2→3 세로 배치를 적용했다. 미등록 초상은 기관 인장으로 대체하며 API·Query·게스트 권한 계약은 유지했다.
- 검증: 명예의 전당·캐릭터 자산 집중 테스트 11건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 2560×1440·1280×720·1024×768·768×1024·390×844에서 초상 로드·DOM 순서·14px 최소 글자·텍스트 잘림·가로 넘침·콘솔 상태 확인
- 관련 커밋: `725910f1`
- 운영 경계: 인증된 로컬 ERP를 읽기 전용으로 확인했으며 라이브 DB·Discord·Dokploy 상태는 변경하지 않았다.

## 2026-08-24 · 안정성 개선 · 기록판 인원별 배치와 운영 HUD 보강

- 기여자가 1명 또는 2명일 때 데스크톱 기록판에 빈 열이 남지 않도록 인원 수 기반 배치를 추가하고, 모바일 2인 카드도 1위 256px·2위 150px 높이로 압축했다.
- 초상 fallback에서 순위를 중복 표시하지 않도록 정리하고 태블릿 이미지 크기 힌트와 1위 LCP 로딩을 보정했다.
- GM 페이지 잠금 컨트롤은 일반 화면 또는 낮은 화면에서 기록판 다음 흐름으로 배치하고 모바일에서는 60px 스위치로 축소했다. 잠금 실패 메시지는 컨트롤을 기준으로 표시되도록 positioned containing block을 유지한다.
- 검증: 명예의 전당 계약 5건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 2560×1440·1280×720·1024×768·800×844·650×844·390×844에서 폭 제한·DOM 1→2→3·초상 로드·가로 넘침·HUD 비겹침·콘솔 경고/오류 없음, UI/UX 및 critical risk review
- 관련 커밋: `5fbdcfb9`
- 운영 경계: 인증된 로컬 ERP를 읽기 전용으로 확인했으며 페이지 잠금 변경, 라이브 DB·Discord·Dokploy 상태 변경은 실행하지 않았다.

## 2026-08-25 · UI 개선 · 연구 공로 기념관 기록판

- `stargate-images`의 환경 배경 워크플로와 마리아 화풍 레퍼런스로 인물·문자·UI가 없는 NOVUS 연구 공로 기념관 배경을 만들고, 연구 도메인의 불투명 WebP 자산으로 발행했다.
- 검은 빈 화면 위의 평면 카드 구성을 기념관 조명 위에 떠 있는 반투명 공로 명부로 바꿨다. 기관 인장, 연속된 기록면, 금·은·동 인레이와 초상 조명을 적용하면서 API·Query·게스트 권한·순위 데이터 계약은 유지했다.
- 2560px에서는 기록판을 1280px로 제한하고, 390px에서는 1→2→3 순서를 유지한 844px 세로 기록판으로 압축했다. 모든 확인 폭에서 초상 3장과 배경 자산이 로드되고 가로 넘침·텍스트 잘림·잠금 HUD 겹침이 없었다.
- 검증: 명예의 전당·자산 집중 테스트 12건, `pnpm assets:audit`, `pnpm assets:test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증된 로컬 ERP 2560×1440·1280×720·1024×768·390×844 브라우저 확인 및 콘솔 경고/오류 0건
- 관련 커밋: `61f81f12`
- 운영 경계: 인증된 로컬 ERP를 읽기 전용으로 확인했으며 페이지 잠금, 라이브 DB·Discord·Dokploy 상태는 변경하지 않았다.

## 2026-08-26 · 기능 확장 · 통합 공적 허브와 자동 헌액 기반

- 기존 연구 TOP 3를 유지하면서 개요·연구·NOVEX·작전 공적·내 리본을 URL 기반 독립 부문으로 통합했다. 게스트는 연구와 NOVEX만, 회원은 공개 U 작전 공적과 본인 소유 AGENT의 리본까지 볼 수 있다.
- NOVEX 확정 시즌 TOP 3를 영구 기록하고, U 작전보고서의 정확히 연결된 플레이어 AGENT만 이중 모델·근거 2개·신뢰도 0.90 기준으로 분석하는 공적 원장과 비활성 기본 writer gate를 추가했다.
- 공개 응답에서는 내부 캐릭터·사용자·보고서 ID, 원문 근거, 모델·신뢰도·감사 필드를 제거하고, 원본 링크도 현재 권한을 재검증하는 불투명 키 경유로 제공한다.
- 검증: Hall 계약 14건, core 36건·worker 152건, manifest 5건, `pnpm typecheck`, `pnpm lint`, `pnpm build:worker`, `pnpm build`, 회원·게스트 2560×1440·1280×720·1024×768·390×844 화면, 콘솔 경고·오류 0건, critical risk review P0/P1 없음
- 관련 구현 커밋: `684efcd7`
- 운영 경계: 라이브 인덱스 생성, Cloud 분석, backfill manifest 생성·적용, writer gate 활성화와 HONOR 알림 발송은 실행하지 않았다.

## 2026-08-26 · 안정성 개선 · 전체 원장 집계와 부분 상태

- 개요의 기록 수를 현재 화면에 로드된 배열 길이가 아니라 공개 가능한 전체 연구 TOP 3·확정 NOVEX 시상 기록·현재 U 원본과 hash가 일치하는 작전 공적의 합계로 계산한다. 게스트 집계는 작전 원장을 조회하지 않는다.
- 전체 집계를 독립 RSC·Query로 분리해 실패해도 연구·NOVEX·작전 부문은 계속 표시하고, 마지막 성공 집계와 재시도 상태를 유지한다.
- 로컬 게스트 화면에서 공개 탭이 `개요·연구 기록·NOVEX`로 제한되고, 현재 데이터가 전체 기록 3건·확정 시즌 0건임을 확인했다. 작전 공적·내 리본은 회원 전용이며 운영 backfill 전에는 임의 기록을 만들지 않는다.
- 검증: Hall 계약 15건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 1280×720·390×844에서 집계 3/0·가로 넘침 0·게스트 탭 제한·신규 콘솔 경고/오류 0건, critical risk review P0/P1 없음
- 관련 구현 커밋: `f0036d11`
- 운영 경계: 라이브 인덱스·DB 쓰기, Cloud 분석, backfill manifest 생성·적용, writer gate 활성화는 실행하지 않았다.

## 2026-08-26 · UI 개선 · 신원조회형 공적 기록 원장

- 개요를 신원조회 화면과 같은 격자·점선·궤도형 인장 문법으로 재구성하고, 장식 왕관 대신 공식 NOVUS ORDO 로고를 기록 좌표와 빈 상태에 적용했다. 개인별 리본 요약은 공용 개요에서 제거하고 별도 `내 리본` 부문에만 유지했다.
- 확정 NOVEX·작전 공적이 없을 때 빈 문구만 보여 주지 않고 현재 연구 누적 1위의 코드네임·누적 CR·기여 횟수를 실제 대표 공적으로 연결했다. 각 원장 채널은 로딩·오류·0건·활성 상태를 구분해 가짜 기록 없이 현재 운영 상태를 설명한다.
- 검증: Hall 계약 15건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증된 GM ERP 1028×1113에서 PIPETTE 6,500 CR·6회와 연구 3명·NOVEX 0시즌·작전 0건 표시, 초상·NOVUS 로고 로드, 가로 넘침 0 확인
- 관련 구현 커밋: `2dfee93a`
- 운영 경계: 라이브 DB·Cloud 분석·backfill·writer gate·알림 상태는 변경하지 않았다.

## 2026-08-26 · 기능 변경 · NOVEX 전 기간 누적 수익 TOP 3

- NOVEX 명예 부문을 확정 시즌 기록에서 전 기간 누적 순위로 전환했다. 모든 `STOCK_SELL`의 숫자형 실현손익과 지급된 `STOCK_DIVIDEND`를 합산하고, 표시 단위로 반올림한 뒤 코드네임·내부 캐릭터 ID 순으로 동률을 결정해 TOP 3만 공개한다.
- 거래 발생 시점의 `ownerId`와 현재 캐릭터 소유자 양쪽에서 GM 및 대소문자 무관 `*TEST` 계정을 제외한다. 공개 API에는 순위·코드네임·누적 확정 수익·수익 이벤트 수만 포함하며 게스트와 회원·GM이 같은 결과를 받는다.
- 시즌 종료 시 NOVEX 공적 원장을 더 이상 생성하지 않고 기존 시즌 알림은 주식 화면으로 연결한다. 구형 시즌 공적은 신원조회·내 리본에서 숨기며, backfill도 NOVEX 시즌 manifest를 새로 만들지 않고 구형 manifest 적용을 첫 mutation 전에 거부한다.
- 로컬 데이터에서는 게스트와 GM 모두 `WD-(𝓃)` 11,450.08 CR → `TIME` 5,974.03 CR → `MARIA` 5,943.44 CR 순으로 확인했다. 1280px 및 390×844에서 가로 넘침이 없었고, 격리된 브라우저의 신규 콘솔 오류는 0건이었다. 외부 Chrome의 한글 문서 확장 프로그램이 `<html>`에 속성을 주입해 만드는 hydration 경고는 애플리케이션 변경과 분리해 확인했다.
- 검증: core 40건·worker 154건, shared-db Hall 계약 7건 통과·DB 통합 1건 환경 미설정으로 skip, Hall·Query·manifest 집중 테스트 21건, `pnpm typecheck`, `pnpm lint`, `pnpm build:worker`, `pnpm build`, critical risk review P0/P1 없음
- 관련 구현 커밋: `cb6dd3a8`
- 운영 경계: 조회 기반 로컬 QA만 수행했으며 라이브 DB 정리·index·backfill·알림·주식 원장 mutation은 실행하지 않았다.

## 2026-08-26 · 성능·안정성 개선 · NOVEX 누적 집계 단일화

- 누적 실현수익 계산을 거래 원장, 거래 당시 사용자, 현재 캐릭터, 현재 소유자를 한 번에 검증하는 단일 Mongo aggregation으로 통합했다. 거래 당시 또는 현재 기준으로 GM·`*TEST`인 계정과 NPC·유효하지 않은 캐릭터는 집계에서 제외한다.
- 개요와 NOVEX 초기 RSC가 같은 렌더에서 요청될 때는 React request cache로 전체 기간 집계를 한 번만 수행한다. 일반 크레딧과 구형 시즌 변경은 Hall을 갱신하지 않고 실제 매도·배당 변경만 NOVEX 개요와 순위를 무효화한다.
- 검증: 실제 MongoDB 7에서 1,000건 초과 원장 fixture 1건, core 40건·worker 156건, Hall 계약 10건·Query key 3건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증된 GM 화면의 실제 연구·NOVEX 데이터와 가로 넘침 0 확인, critical risk review P0/P1 없음
- 관련 구현 커밋: `4f4a5091`
- 운영 경계: 조회 기반 로컬 QA만 수행했으며 라이브 DB·주식 원장·realtime 운영 설정은 변경하지 않았다. 실제 Mongo 통합 fixture의 상시 CI 실행과 별도 API 요청 사이의 전 기간 집계 공유는 후속 성능 과제로 남겼다.

## 2026-08-27 · UI 개선 · 라운드 공적 캐러셀과 통합 원장 채널

- 개요의 직사각형 연결형 기록면을 15~28px 라운드 카드 체계로 바꾸고, 데스크톱·태블릿·모바일에 남아 있던 구형 `border-right: 0` 연결 규칙을 제거해 각 카드의 네 면과 모서리를 온전히 표시한다.
- 대표 공적은 작전 공적 → NOVEX 누적 1위 → 연구 누적 1위 → 추가 작전 공적 → 중복되지 않는 내 리본 순으로 구성되는 캐러셀로 통합했다. 6.5초 자동 순환, 이전·다음·인디케이터, 44px 터치 스와이프, 수동 정지와 hover·focus·touch 일시 정지를 제공하며 reduced-motion 환경은 수동 모드로 고정한다.
- `ARCHIVE CHANNELS`에는 회원·GM의 `MY RIBBONS`를 추가해 연구·NOVEX·작전·개인 원장 상태를 한 화면에 표시한다. 로컬 GM 데이터는 연구 3명·NOVEX 3명·작전 0건·내 리본 0건이므로 캐러셀에는 실제 기록이 있는 연구와 NOVEX만 노출되며 빈 기록을 임의 생성하지 않는다.
- 검증: Hall 계약 15건, `pnpm typecheck`, `pnpm lint`, 변경 파일 ESLint, `pnpm build`, 인증된 GM ERP 2056×1113·390×844에서 자동 순환·라운드 테두리·터치 영역·가로 넘침 0 확인, 확장 없는 브라우저 콘솔 경고·오류 0건
- 관련 구현 커밋: `d46c9428`
- 운영 경계: 조회와 로컬 UI 제어만 수행했으며 라이브 DB·Cloud 분석·backfill·writer gate·알림 상태는 변경하지 않았다.

## 2026-08-27 · 데이터 이관 · 작전 공적 원장 최초 헌액

- U 등급 작전보고서 18건을 수동 검토 manifest로 고정하고, 정확히 연결된 플레이어 AGENT만 대상으로 작전 공적 11건을 `honor_records`에 멱등 반영했다. 보고서 12건에는 누락된 관련 인물 연결 137개와 출처 provenance를 원자적으로 보강했다.
- `honor_records`·`honor_analysis_states`의 필수 인덱스 8개를 생성하고, 분석 상태 18건이 모두 `SUCCEEDED` 및 `operation-honor-manual-v1` revision인지 재조회했다. 활성 작전 공적은 11건이며 원본 연결 누락은 0건이다.
- 인증된 로컬 GM ERP의 `/erp/hall-of-fame?view=operations`에서 MARIA·PIPETTE·네베드 등 11건, 6개 부문 필터와 불투명 원본 링크를 확인했다. 빈 상태는 사라졌고 브라우저 콘솔 오류는 0건이었다.
- 검증: Hall 계약 15건, worker 158건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인덱스 재점검 `missing 0`·`conflicting 0`, critical risk review P0–P2 없음
- 관련 구현 커밋: `54d73a15`
- 운영 결과: HONOR 알림 0건·웹훅 0건으로 과거 이관의 무발송 계약을 지켰고, `HALL_OF_FAME_V2_WRITES_ENABLED` gate는 비활성 상태로 유지했다. 크레딧·인벤토리·주식 원장은 변경하지 않았다.

## 2026-08-28 · 표시 개선 · 작전 공적 원본 링크와 확정 상태 보정

- 작전 공적의 `기록 보기` 링크를 브라우저 기본 파란색 대신 NOVUS 골드 토큰, 라운드 테두리, 명시적 외부 이동 표식을 사용하는 버튼형 링크로 통일했다. 모바일에서도 같은 시각 언어와 터치 영역을 유지한다.
- 수동 검토로 확정된 `operation-honor-manual-v1` 분석 상태도 현재 보고서 원본 해시가 일치하면 완료로 인정해, 보고서 상세에서 확정 공적이 잘못 `공적 재심사 중`으로 표시되던 문제를 수정했다. 자동 확정으로 오해할 수 있던 안내 문구는 검토·확정 방식에 맞게 정리했다.
- 현재 원장을 읽기 전용으로 재점검한 결과 활성 작전 공적 11건·수상 보고서 8건·분석 상태 18건을 확인했고, 논리키 중복·근거 감사 필드 미달·잘못된 공개 키·원본 링크 실패는 모두 0건이었다.
- 검증: Hall 계약 16건, 수동 검토 도구 15건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증된 GM ERP에서 작전 공적 11개 원본 링크·8개 보고서 공적 패널·PIPETTE 신원조회 리본 2건과 콘솔 오류 0건 확인
- 관련 구현 커밋: `6d4a3206`
- 운영 경계: 라이브 DB 쓰기·알림·웹훅은 수행하지 않았고, 향후 보고서 자동 분석을 담당하는 `HALL_OF_FAME_V2_WRITES_ENABLED` gate는 비활성 상태로 유지했다.

## 2026-08-28 · 데이터 보강 · 작전 공적 추가 헌액

- 전체 U 작전보고서 18건을 엄격 기준으로 다시 검토해, 서로 다른 직접 근거 2개와 정확한 플레이어 AGENT 연결을 모두 만족한 OTILIA의 `810번 도서 인원 회수 공적`과 INDEXER의 `왕관 감염 격리 공적`을 추가했다. 두 번째 독립 근거가 부족한 후보는 헌액하지 않았다.
- 기존 활성 공적 11건은 그대로 유지하고 신규 2건만 멱등 반영해 작전 공적 원장을 13건으로 보강했다. 인증된 GM 명예의 전당에서 13건과 두 신규 카드의 부문·제목·공적 문구·원본 이동을 재확인했다.
- 향후 보고서 자동 분석 gate는 worker 런타임에 필수 모델 자격증명이 없어 활성화하지 않았다. 과거 헌액 적용 과정의 HONOR 알림과 Discord·웹훅 발송은 모두 0건이다.
- 검증: 수동 검토·manifest 테스트 15건, Hall 계약 16건, core 41건·worker 158건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 원본 보고서·신원조회 교차 확인
- 관련 구현 커밋: `5b9db3d9`, `9c1de074`
- 운영 결과: 원본 해시와 분석 revision을 재검증한 신규 2건만 적용했으며 크레딧·인벤토리·주식 원장은 변경하지 않았다.

## 2026-08-28 · 아키텍처 변경 · 로어 검토 기반 작전 공적 원장

- 작전 공적의 외부 Ollama 제안·검토 모델과 worker consumer를 제거하고, `stargate-lore`가 U 보고서 원문과 정확히 연결된 플레이어 AGENT를 직접 검토하는 단일 흐름으로 전환했다. 보고서당 최대 3건·인물당 1건·서로 겹치지 않는 원문 근거 2개·6개 고정 부문 계약은 그대로 유지한다.
- 저장된 검토 계획에서 hash-locked manifest를 만드는 읽기 전용 명령, 명시적 승인 manifest만 적용하는 명령, 현재 보고서·검토 상태·활성 원장을 비교하는 상태 감사를 분리했다. 감사 결과는 수상자·logicalKey·부문·공개 문구·근거·source hash까지 비교하고 삭제·V 이상 원본의 고아 활성 기록도 별도로 탐지한다.
- 명예의 전당과 보고서 상세는 자동 분석·재시도 표현 대신 `작전 공적 검토 대기`를 표시한다. 신규 헌액 알림은 적용 시 `--notify-new`를 별도 지정한 경우에만 한 번도 발급되지 않은 논리키에 정확히 1회 생성한다.
- 검증: core 44건·worker 132건, Hall·수동 검토·manifest 집중 테스트 28건, shared 공적 계약 6건 통과·DB 통합 1건 환경 미설정 skip, `pnpm typecheck`, `pnpm lint`, `pnpm build`, critical risk review P0–P2 없음
- 관련 구현 커밋: `d94e34c3`
- 운영 경계: 읽기 전용 상태 점검에서 U 보고서 18/18 `CURRENT`, 계획·활성 공적 13/13, 원장 드리프트·고아 기록 0건을 확인했다. 라이브 DB 쓰기, manifest 적용, HONOR 알림, Discord·웹훅은 실행하지 않았다.
