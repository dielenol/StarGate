# `/erp/equipment-shop/simulator` 작업 이력

## 2026-07-28 · 기능 개선 · 배치 흐름과 화기 사격 방향

- 적 위치를 먼저 지정하면 내 위치 조정으로 자동 전환되고, 이후 전투판 클릭으로 내 위치를 자유롭게 옮길 수 있도록 배치 흐름을 변경
- 전투판 토큰과 안내를 `나`·`적`으로 명확히 구분하고 적 위치 재지정 동작을 제공
- 화기·설치화기·특수화기의 사격을 같은 가로줄 또는 세로줄로 제한하고, 대각선 배치에는 직선 정렬 안내를 표시
- 검증: `node --test lib/equipment-shop/__tests__/simulator.test.mjs` 11/11 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저에서 데스크톱 및 390px 모바일 클릭 흐름·가로/세로/대각선 판정·콘솔 오류 확인
- 관련 커밋: `88452bf`

## 2026-07-28 · 기능 개선 · 캐릭터 토큰과 턴 종료 연출

- `현재 배치 기준 비교` 영역을 제거하고 공격 로그가 하단 전체 폭을 사용하도록 단순화
- 로그인한 사용자의 주 캐릭터 픽셀 프로필을 `내 캐릭터` 토큰으로 표시하고, 이미지가 없을 때는 `나` 대체 토큰을 제공
- `nochichim`의 턴 종료 HUD 타이밍과 효과음을 이식하고 다음 턴 안내음과 겹치지 않도록 조정
- 데스크톱과 모바일에서 전투 조작 버튼이 전투판 내부에 줄바꿈되도록 반응형 배치를 보강
- 검증: 시뮬레이터 테스트 15/15 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 효과음 원본 바이트 일치 및 로컬 응답 200 확인, 인증된 로컬 브라우저 1280px·390px에서 턴 종료 클릭·HUD 표시/종료·가로 넘침·콘솔 오류 확인
- 관련 커밋: `228df2a`

## 2026-07-28 · 버그 수정 · 캐릭터 이미지 토큰 연결

- 전투판의 내 캐릭터 토큰이 `pixelCharacterImage`의 SD 전신 이미지를 우선 사용하고, 픽셀 프로필과 `나` 텍스트 순으로 폴백하도록 수정
- 레지스트라 캐릭터의 DB 이미지 필드가 비어 있거나 누락된 환경에서도 저장소의 확정된 픽셀 프로필·SD 에셋을 사용하도록 보강
- 플레이어블 캐릭터 17명의 핵심 이미지 PNG/WebP 쌍, 동일 해상도, 알파 채널, 토큰 슬러그 매핑을 자동 검사
- 검증: 관련 테스트 17/17 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저 1280px·390px에서 레지스트라 SD 토큰 로드·가로 넘침·콘솔 오류 확인
- 관련 커밋: `7ae1a96`

## 2026-07-28 · 버그 수정 · 전투 토큰과 상태 UI 통합

- 작은 전투판에서는 SD 전신보다 픽셀 프로필을 우선 사용하고, 메인 캐릭터 연결이 없어도 레지스트라 또는 저장소에 매핑된 계정 이름이면 확정 토큰 에셋을 사용하도록 수정
- `nochichim` 전투 토큰을 기준으로 원형 아바타, `나`·`적` 이름표, HP 바, 상태이상 아이콘과 HP·SAN·ATK/DEF 상세 팝업을 전투판에 통합
- 오른쪽의 중복 캐릭터·적 상태 카드를 제거하고 사거리별 피해와 무기 운용 정보만 유지
- 이미지가 없는 캐릭터는 다른 캐릭터 에셋을 잘못 붙이지 않고 계정 첫 글자와 `이미지 미등록` 상태를 표시
- 검증: 관련 테스트 18/18 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저에서 J 이미지 미등록 토큰·레지스트라 픽셀 프로필 로드·공격 후 적 HP 토큰 갱신·중복 상태 카드 제거·콘솔 신규 오류 없음 확인
- 관련 커밋: `8c15de5`

## 2026-07-29 · 기능 개선 · 전술 토큰 이동과 중기관총 다이아몬드 사거리

- 주 캐릭터와 확정 이미지 매핑이 모두 없는 사용자는 섹터 C 현장요원 이미지를 기본 토큰으로 사용하고, 적 훈련 표적에는 일반전투원 이미지를 적용
- 브라우저 기본 드래그 대신 포인터 위치를 기준으로 토큰을 실제 이동하며, 중기관총 설치 중에는 내 토큰 이동을 잠그고 해체 후 다시 이동 가능하도록 변경
- 중기관총 설치와 해체에 각각 1턴을 소모하고, C1 기준 A3·B4·C5·D4·E3처럼 가로·세로 이동 칸 합계 4칸 이내를 사격하는 다이아몬드 사거리를 적용
- 선택 장비의 공격 가능 셀을 전투판에 강조하고, 1280px 화면에서도 장비 룰 카드가 조작 버튼을 덮지 않도록 반응형 전환 폭을 보정
- 검증: 관련 테스트 20/20 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저에서 중기관총 설치 1→2턴·해체 2→3턴, C1→A3 공격 후 적 HP 60→52·탄환 10→9, 토큰 포인터 이동, NPC 이미지 로드 및 신규 콘솔 오류 없음 확인
- 관련 커밋: `96a18fd`

## 2026-07-29 · 기능 개선 · 5×5·1×5·5×1 전장 선택

- 기존 5×5 표준 전장을 기본값으로 유지하면서 1×5 세로 전장과 5×1 가로 전장을 선택할 수 있는 전장 규격 버튼을 추가
- 전장 변경 시 각 규격의 유효 좌표 안에서 기본 배치를 구성하고 턴·장비 자원·중기관총 설치 상태·표적 상태를 함께 초기화
- 전장별 열·행 수에 맞춰 좌표 라벨, 셀, 공격 가능 범위와 토큰 배치를 가변 렌더링하고 모바일에서도 가로 넘침 없이 표시
- 검증: 관련 테스트 19/19 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저에서 5×5 25칸(C1→C3), 1×5 5칸(A1→A3), 5×1 5칸(A1→C1), 전환 후 1턴 초기화와 390px 가로 넘침 없음 및 신규 콘솔 오류 없음 확인
- 관련 커밋: `da4cd8a`

## 2026-07-29 · 시각 개선 · 적색 사거리와 공통 전장 크기

- 공격 가능 셀을 기존 금색 음영에서 고채도 적색 사선 패턴, 적색 테두리와 내부 광택으로 변경해 전투판에서 즉시 식별 가능하도록 강화
- 1×5 전장의 단일 열이 5×5 전체 가로폭을, 5×1 전장의 단일 행이 5×5 전체 세로높이를 사용하도록 확대
- 검증: 관련 테스트 19/19 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인증된 로컬 브라우저에서 세 전장 모두 데스크톱 `914×476px`, 390px 모바일 `280×420px` 보드 영역 일치, 적색 계산 스타일·가로 넘침 없음 및 신규 콘솔 오류 없음 확인
- 관련 커밋: `ca82b7b`

## 2026-07-29 · 기능 개선 · 장착 무기 우선 장비 목록

- 로그인한 사용자의 주 캐릭터 인벤토리에서 현재 장착 중인 무기를 읽어 훈련장 장비 목록 최상단에 표시
- 훈련 규칙이 있는 장착 무기는 실제 아이템명과 이미지를 유지한 채 초기 선택하고, 규칙이 없는 전용·커스텀 무기는 숨기지 않고 `훈련 규칙 미등록` 비활성 항목으로 표시
- 주 캐릭터나 장착 무기가 없거나 인벤토리 조회가 실패하면 기존 보급형 권총 기본 선택과 전체 보급 장비 목록을 유지
- 검증: 관련 테스트 21/21 통과, `pnpm typecheck`, `pnpm lint`, `git diff --check` 통과, 인벤토리가 비어 있는 인증된 로컬 계정에서 보급 장비·권총 폴백, 가로 넘침 없음 및 신규 콘솔 오류 없음 확인
- 관련 커밋: `fdcae55`

## 2026-07-29 · 기능 개선 · 원거리 장비 규칙과 화상 로어 동기화

- 원거리 장비의 사거리·탄환·특수행동·상태이상 규칙을 장비 명세, 기본 카탈로그와 훈련장 룰 카드에 동기화
- 화염방사기의 화상을 3라운드 동안 지속 피해 5·방어력 -5를 적용하는 상태로 표시하고, 훈련장에서 라운드 경과와 유효 방어력을 계산
- 소이선 화염 지대의 3라운드 유지, 화상 부여와 턴 경과를 훈련장 전투판에서 재현
- 검증: 관련 테스트 28/28 통과, `pnpm typecheck`, `pnpm lint`, 장비 명세 스키마 46/46, payload JSON 파싱, lore 공개 문구 검사, `git diff --check` 통과, 인증된 로컬 브라우저에서 화상 규칙 노출·오버플로 없음·콘솔 오류 없음 확인
- 관련 커밋: `14693ae`

## 2026-07-29 · 규칙 정정 · 화염방사기 고정 화상 수치 적용

- 임시로 기록됐던 가변 N·회복 전 지속 규칙을 사용자 원문에 맞춰 3라운드·지속 피해 5·방어력 -5로 정정
- 장비 명세, 기본 카탈로그, focused payload, 훈련장 상태 처리와 계약 테스트를 동일한 고정 수치로 통일
- 검증: 관련 테스트 28/28 통과, `pnpm typecheck`, 대상 ESLint, focused payload dry-run, `git diff --check` 통과
- 관련 커밋: `590df76`

## 2026-07-29 · 규칙 보완 · 원거리 특수 효과 계산 완성

- 멍함이 유지되는 동안 원거리 공격 피해를 20% 감소시키는 계산을 규칙 엔진에 반영
- 중기관총 광역 난사를 대상마다 별도 1d6 판정하도록 구성하고, 현재 단일 표적 훈련장에도 같은 처리 경로를 연결
- 소이선이 전방 3칸뿐 아니라 선택한 영역 하나에도 화염 지대를 만들 수 있도록 범위 계산을 보완
- 검증: 관련 테스트 29/29 통과, `pnpm typecheck`, 대상 ESLint, `git diff --check` 통과, 인증된 로컬 브라우저에서 대각선 선택 영역 D2에 소이선 3라운드·화상 3R·유효 DEF 0 적용과 콘솔 오류 없음 확인
- 관련 커밋: `fc014d5`

## 2026-07-29 · 규칙 재정정 · 최신 화상 원문 복원

- 고정 3라운드·피해 5·방어력 -5 규정을 폐기하고, 회복 적용 전까지 매 라운드 N의 지속 피해와 방어력 -N을 적용하는 최신 사용자 원문으로 복원
- 화상 중복 시 방어력 감소 -N이 누적된다는 규정을 장비 명세, 기본·focused payload와 훈련장 룰 카드에 다시 통일
- 출처에 없는 N을 임의 계산하지 않도록 훈련장에서는 화상 상태만 회복 전까지 유지하고 지속 피해·방어력 감소 자동 계산은 보류
- 검증: 관련 테스트 29/29 통과, `pnpm typecheck`, `pnpm lint`, lore 공개 문구 검사와 구 화상 문구 전역 검색 통과
- 관련 커밋: `ea63c7c`
