# `/erp/shop` 작업 이력

## 2026-07-30 · 버그 수정 · 띠아 대사 HUD 상시 표시

- 편의점 영업 종료 상태에서도 간략 폐점 안내 대신 띠아의 전체 대사 HUD와 표정을 유지하도록 통일했다.
- 데스크톱·모바일의 운영 잠금 컨트롤을 HUD 위로 이동해 대사창과 겹치지 않도록 했다.
- 검증: `pnpm lint`, focused ESLint, `git diff --check`, 인증 브라우저 1280×720·390×844 확인(가로 넘침·고정 UI 충돌·콘솔 오류 없음)
- 관련 커밋: `7ea4903`
- 후속 작업: 전체 `pnpm typecheck`는 별도 작업 중인 `equipment-shop/simulator/EquipmentSimulatorClient.tsx`의 null 가능성 오류로 차단됨

## 2026-07-30 · 기능 추가 · 띠아 신제품 웹훅

- 신제품 출시 알림을 편의점 전용 Discord 웹훅으로 전달하고, 띠아가 상품명·분류·가격·효과와 편의점 링크를 안내하도록 추가했다.
- 사용자 입력 문구의 Discord 멘션을 무력화하고, 외부 전송 실패는 상품 출시와 분리된 outbox 재시도 대상으로 처리했다.
- 검증: 집중 테스트 14개, worker 전체 테스트 38개, `pnpm typecheck`, 대상 ESLint, `pnpm build`, `git diff --check`
- 관련 커밋: `068e443`
- 후속 작업: 운영 worker의 `WORKER_OUTBOX_KINDS` opt-in 전환 전에는 알림이 `PENDING`으로만 적재된다.

## 2026-07-30 · 기능 개선 · 띠아 신제품 이미지·대사

- 신제품 공지에 등록된 상품 이미지를 크게 표시하고, 이미지가 없거나 안전하지 않은 경로면 공지를 실패시키지 않고 이미지 없이 발송하도록 했다.
- 띠아가 신제품을 직접 소개하고 권하는 말투로 출시 대사를 보강했다.
- 검증: 웹 계약 테스트 7개, worker 전체 테스트 39개, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`
- 관련 커밋: `54631c1`

## 2026-07-30 · 버그 수정 · 강제 종료 자동 해제

- GM 강제 종료를 영구 폐점 상태로 두지 않고, 설정 이후 첫 정상 개점 시각(한국시간 월~토 06:00)에 자동 운영으로 복귀하도록 변경했다.
- 서버 렌더 직후와 06:00·20:00 영업 경계마다 카탈로그를 다시 확인해, 화면을 계속 열어둔 경우에도 영업 상태가 자동 갱신되도록 했다.
- worker의 띠아 입고 웹훅도 웹과 같은 영업 판정을 사용해 만료된 강제 종료를 셔터 상태로 안내하지 않도록 통일했다.
- 검증: 영업 일정 집중 테스트 7개, core 전체 테스트 7개, worker 전체 테스트 44개, `pnpm typecheck`, 대상 ESLint, `git diff --check`, 인증 브라우저 1280×720 확인(가로 넘침·콘솔 오류 없음)
- 관련 커밋: `b90d1dc`

## 2026-07-31 · 성능 최적화 · 재고 refresh 웜패스 단락

- 카탈로그 조회가 매번 전 품목 refresh를 재수행하던 것을, 전 재고 1회 선조회 후 오늘 갱신분을 제외하고 stale 품목만 갱신하도록 바꿨다 (웜패스 DB 왕복 약 48회→1회, 일일 크론이 본 갱신 담당).
- 페이지와 catalog API의 중복 응답 조립을 `shop/_data.ts` 공유 빌더로 통합하고 영업 상태 조회를 refresh 체인과 병렬화했다.
- 검증: 웜패스 단락 테스트 7건 신규 + shop 스위트 44개(100-동시성 race 포함), `pnpm lint`, `pnpm typecheck`, `pnpm build`
- 관련 커밋: `58ccb35`

## 2026-07-31 · 기능 추가 · 미스터비스트 소다 복권 이벤트

- 활성 지급 기간에 미스터비스트 소다 구매 수량만큼 캐릭터 귀속 스크래치 복권을 함께 지급하고, 마우스·터치 긁기 또는 접근성 버튼으로 결과를 확인하도록 추가했다.
- 꽝부터 0등까지 7단계 확률표를 적용했으며, 0등은 `0.0001%` 확률로 `100,000 CR`을 지급하고 1등보다 강한 전용 광원·파티클 효과를 표시한다.
- 복권 권리를 이벤트·확률표 버전에 귀속된 1장 단위 원장으로 관리하고, 종료 후 사용·중복 요청·다중 탭·소유권 변경·거래 우회·필수 인덱스 누락을 안전하게 처리한다.
- 2등·1등·0등은 현재 공개 캐릭터만 최근 당첨 피드에 표시하며, 서버의 진행 중 복권을 기준으로 이어서 사용할 수 있다.
- 검증: 복권·거래 집중 테스트 15개, `pnpm typecheck`, `pnpm lint`, shared-db build, `pnpm build`, `git diff --check`, 인증 브라우저 데스크톱·390×844 확인(오류 오버레이·콘솔 경고·가로 넘침 없음)
- 관련 커밋: `e733a98`, 운영 스펙 `8962f0a`
- 후속 작업: 운영 활성화 전에 legacy 상태 점검, 필수 인덱스 적용, 비공개 master item dry-run 페이로드 반영 및 재조회가 필요하다.

## 2026-07-31 · 기능 개선 · 소다 일일 제한과 이벤트 포스터

- 미스터비스트 소다를 로그인 사용자 기준 한국시간 하루 최대 10개로 제한하고, 여러 주문·동시 요청에서도 10개를 넘지 않도록 결제 트랜잭션에 일일 카운터를 결합했다.
- 이벤트 활성 기간에는 소다와 복권 이미지를 사용한 전용 포스터를 노출하고, 소다 1개당 복권 1장·일일 제한·0등 100,000 CR 정보를 함께 안내한다.
- 이벤트 종료 경계와 결제 응답 유실이 겹쳐도 소다만 결제되거나 중복 결제되지 않도록 복권 기대조건과 장바구니 멱등 키 보존을 추가했다.
- 검증: 집중 테스트 22개 중 18개 통과·Mongo 통합 4개 환경 부재로 미실행, 대상 ESLint, `git diff --check`, 프로덕션 빌드, 인증 브라우저 1280×720·390×844 확인(가로 넘침·콘솔 오류 없음)
- 관련 커밋: `0b26f6e`
- 후속 작업: 이벤트 활성화 전에 격리 replica-set MongoDB에서 동시성·rollback·멱등 replay 통합 테스트 4개를 실행하고, 제한 적용 전 같은 KST 일자의 기존 소다 구매가 없도록 이벤트 시작 전에 배포한다.

## 2026-07-31 · 기능 개선 · 복권 이벤트 GM 운영 설정

- 환경변수 기반 이벤트 활성화를 제거하고, 편의점의 GM 전용 메뉴에서 이벤트 ID·시작/종료 시각(KST)·활성 상태를 직접 저장하도록 변경했다.
- DB 설정 문서가 없으면 안전하게 비활성화하며, 활성화 전 필수 인덱스와 비공개 복권 마스터 아이템 준비 상태를 화면과 API에서 확인한다.
- 동시 GM 저장은 설정 버전 CAS로 차단하고, 설정 변경·감사 outbox·결제 시점 활성 상태·캐릭터 소유권을 MongoDB transaction과 write fence로 보호한다.
- 검증: shop 전체 테스트 64개 및 집중 테스트 17개, 대상 ESLint, `pnpm typecheck`, `pnpm build`, `git diff --check`, 인증 브라우저 1280×720·390×844 확인(가로 넘침·콘솔 오류 없음)
- 관련 커밋: `6021161`
- 후속 작업: 운영 활성화 전에 별도 승인 범위에서 필수 인덱스와 canonical 비공개 master item을 준비하고, 격리 replica-set 환경에서 checkout↔비활성화·소유권 이전·감사 outbox 실패 경쟁 테스트를 실행한다.

## 2026-07-31 · 기능 개선 · 복권 이벤트 안전 미리보기

- GM 설정에 이벤트 포스터·긁기 전·꽝부터 0등까지 플레이어 화면을 버튼으로 전환하는 안전 미리보기를 추가했다.
- 미리보기는 복권 소모·추첨·크레딧 지급·설정 저장 API와 분리하고, 준비 상태와 관계없이 연출만 확인할 수 있도록 했다.
- 필수 준비 항목을 중복 지급·사용 방지용 DB 인덱스 5개와 인벤토리 표시용 비공개 복권 아이템으로 풀어 쓰고, 읽기 전용 재확인 버튼을 추가했다.
- 검증: shop 테스트 64개, 대상 ESLint, `pnpm typecheck`, `pnpm build`, `git diff --check`, 인증 브라우저 1280×720·390×844 확인(포스터·0등 연출·가로 넘침·콘솔 오류 없음)
- 관련 커밋: `90ca42a`

## 2026-07-31 · 기능 개선 · 복권 운영 기반 원클릭 준비

- GM 복권 설정에 `운영 기반 한 번에 준비` 버튼을 추가해 복권 전용 인덱스 5개와 상점에 노출되지 않는 복권 마스터 아이템을 최초 1회 준비하도록 했다.
- 준비 작업은 이벤트를 자동 활성화하지 않으며, GM 확인 뒤에만 실행된다. 이미 준비된 상태에서는 변경이나 중복 감사를 만들지 않는다.
- 장시간 인덱스 생성과 부분 실패를 작업 임대·heartbeat·재시도로 복구하고, 마스터 아이템·준비 완료 상태·GM 감사 outbox를 하나의 transaction으로 확정한다.
- 검증: shop 테스트 65개, 집중 테스트 17개, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, critical risk review, 인증 브라우저 데스크톱·390×844 확인(버튼 노출·오류 오버레이·콘솔 오류·가로 넘침 없음)
- 관련 커밋: `4192b0b`

## 2026-08-01 · 버그 수정 · 복권 고액 당첨 채널 공지

- 공개 캐릭터가 미스터비스트 복권 2등·1등·0등에 당첨되면 당첨 확정과 함께 편의점 Discord 공지를 durable outbox에 예약하도록 누락된 전달 경로를 연결했다.
- 띠아 이름·아바타의 고액 당첨 카드로 당첨 캐릭터·등수·보상을 안내하고, 발송 직전에도 캐릭터 공개 상태를 재확인해 비공개 정보는 채널에 노출하지 않는다.
- 검증: 복권/outbox 집중 테스트 15건, worker 테스트 47건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, critical risk review
- 관련 커밋: `4784958`
- 후속 작업: 실제 전달 전 worker에 `MRBEAST_LOTTERY_WINNER_WEBHOOK` kind를 opt-in하고 편의점 webhook secret을 확인한 뒤 PENDING drain을 검증해야 한다.

## 2026-08-01 · 기능 추가 · 소다 판매량 STM 시세 연동

- 복권 이벤트 시작부터 최대 14일 동안 미스터비스트 소다 판매 수량을 결제·재고·크레딧과 같은 transaction에서 주가 수요 원장에 기록하도록 추가했다.
- 이벤트 포스터에 `판매 1개당 STM +0.10%p` 안내를 표시하고, 원장 준비에 실패하면 결제 전체를 rollback해 판매량 누락을 막는다.
- 기존 판매량 이관은 기본 dry-run이며, 기간 경계·설정 버전·DB·tick gate를 검증한 뒤에만 원자적으로 실행할 수 있다. 읽기 전용 운영 집계는 9명·64개였고 DB는 변경하지 않았다.
- 검증: core 테스트 10개, focused web 테스트 10개, worker 테스트 47개, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, critical risk review, 인증 브라우저 1280×720·390×844 확인(배지 노출·가로 넘침·콘솔 오류 없음)
- 관련 커밋: `eff30f76`
- 후속 작업: 격리 replica-set용 `TEST_MONGODB_URI`가 없어 rollback/replay 통합 테스트 1개는 skip됐다. 라이브 배포·backfill·Web/worker gate 활성화는 배포 런북 순서와 별도 운영 승인이 필요하다.

## 2026-08-02 · 버그 수정 · 일요일 자동 영업

- 일요일 종일 마감 예외를 제거하고 요일과 관계없이 매일 한국시간 06:00 이상 20:00 미만에 자동 개점하도록 변경했다.
- 토요일 강제 종료도 일요일 06:00에 만료되며, 일요일 영업 중 다음 상태 갱신 경계는 당일 20:00이 되도록 보정했다.
- 화면과 결제 API의 폐점 안내를 `매일 06:00~20:00`로 통일했다.
- 검증: 영업 일정 테스트 8개, core 테스트 10개, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, 인증 브라우저 일요일 확인(폐점 장면 없음·상품 17개 담기 활성·가로 넘침·콘솔 오류 없음)
- 관련 커밋: `550c60be`

## 2026-08-06 · 기능 변경 · 미스터비스트 소다 불량 판정

- 소다를 사용할 때 단위별로 판정해 80%는 HP/SAN 10, 20%는 불량 소다로 HP/SAN 1을 회복하도록 서버 결과를 저장한다.
- ERP와 Nochichim 재전송은 최초 판정 결과를 재사용하며 차감·회복·알림·캐시 갱신이 중복되지 않도록 연결했다.
- 검증: 소다·소비·VTT 집중 테스트, `pnpm typecheck`, `pnpm lint`, `pnpm build`
- 관련 커밋: `30ffe959`
- 후속 작업: 라이브 소다 소비 mutation은 실행하지 않았다.

## 2026-08-07 · 대사·오디오 개선 · 띠아 오류 응대

- 품절·재고 부족·일일 소다 제한·복권 종료·재주문·결제 오류 대사를 밝고 바쁜 직원 말투 안에서 서로 다르게 다듬고, 판매 이력 같은 근거 없는 설정은 만들지 않았다.
- 사용자 선택과 실패 응답은 TIA 비프로 재생하고 최초 정적 인사와 대표 캐릭터 없음 상태는 무음, 폐점 내레이션은 기존 `system` 비프를 유지했다.
- 검증: 띠아 계약 테스트 포함 집중 테스트 54/54, `pnpm dialogue:lint`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증 브라우저 데스크톱 재고 부족 선택 및 390×844 가로 넘침·콘솔 오류 없음 확인
- 관련 커밋: `10f0a43f`
- 운영 경계: 구매·재주문·복권·재고 mutation은 실행하지 않았다.

## 2026-08-09 · 안정성 개선 · 발주 접수·입고 원장

- 발주 `REQUESTED → FULFILLED` 단계를 중앙 workflow 채널에 연결하고, 요청 저장·입고 재고 변경·각 webhook outbox를 해당 transaction에서 함께 확정한다.
- 개선 전 생성된 pending 발주는 입고 transaction에서 deterministic REQUESTED 이벤트를 먼저 보강한 뒤 FULFILLED를 기록해 대량 startup backfill 없이 원장을 복구한다.
- 편의점·주식 자동 전달의 소유자를 장기 실행 worker로 정리하고 웹 경로는 인증된 수동 복구로만 남겼다.
- 검증: 거래·발주 계약 16건, restock·stock 집중 테스트, worker 62건, 웹 `typecheck`·`lint`·production build, critical risk review
- 관련 커밋: `45944a0c`
- 운영 경계: 라이브 발주·입고·재고·복권 outbox 발송은 실행하지 않았다.

## 2026-08-10 · 안정성 개선 · 입고 카드 무중단 교체

- 웹의 편의점 Discord 직접 생성·삭제 경로를 제거하고 desired-state 기록은 웹, 실제 전달은 worker 한 곳만 담당하도록 정리했다.
- 입고 공지는 새 메시지를 먼저 생성·활성화한 뒤 기존 메시지를 정리해 Discord 오류나 재시작 중에도 기존 카드가 먼저 사라지지 않는다.
- 캐릭터 공개 상태가 해제된 복권 공지 등 정책상 생략된 전달을 실제 발송과 분리해 관리자 현황에서 구분한다.
- 검증: 편의점·주식·관리자 집중 테스트 41건, worker 71건, 웹 `typecheck`·전체 `lint`, 엄격 코드 리뷰
- 관련 커밋: `1ef1ef5a`
- 운영 경계: 라이브 입고·재고·복권 상태와 Discord 메시지는 변경하지 않았다.
