# /erp/admin/equipment-workshop

## 2026-07-28 · 기능 추가 · 장착형 어빌리티 강화 견적

- 공방 견적 결과에 대상 능력 코드와 대체 효과를 구조화해 입력·편집하는 화면을 추가했다.
- `악식의 콘치타 - 개조형` 프리셋에 근거리 15·중거리 5 물리 피해, 절제 10×5, 500 CR, 4,320분 제작 사양을 등록했다.
- 중복 코드와 빈·과도하게 긴 효과를 거부하고, 견적 발행 및 수락 시 대상 캐릭터의 능력 코드가 유지되는지 재검증한다.
- 검증: 공방 집중 테스트 45건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증된 로컬 관리자 화면에서 대상 요청을 읽기 전용으로 확인
- 관련 커밋: `75be2ab`

## 2026-07-28 · 버그 수정 · 접수 단계 편집 화면 복구

- `REQUESTED` 제작·강화 요청에서도 2~6단계 설계·비용·재료·결과·최종 검토 화면을 다시 표시한다.
- 서버 상태 규칙에 맞춰 `검토 시작` 전에는 견적 발행을 비활성화하고 제출 가드와 안내 문구를 제공한다.
- 검증: 공방 집중 테스트 36건, `pnpm typecheck`, `pnpm lint`, `git diff --check`, 인증된 로컬 관리자 화면 2056×1147에서 `TIGER298 / 악식의 콘치타 / REQUESTED` 요청의 2~6단계 표시 및 콘솔 오류 0건을 읽기 전용 확인
- 관련 커밋: `dbe7bd4`

## 2026-07-28 · 시각 수정 · 콘치타 개조형 누끼

- 공방 프리셋과 견적 미리보기의 `악식의 콘치타 - 개조형` 이미지를 투명 WebP로 교체해 흰 배경이 남지 않게 했다.
- 검증: 1086×1448 sRGBA, 투명 모서리·불투명 단검 영역 확인, 로컬 브라우저 정적 자산 렌더와 콘솔 오류 0건 확인
- 관련 커밋: `52432cd`

## 2026-07-30 · 기능 변경 · 완료 결과 보관과 재료 독립 견적

- 완료·반려·취소 요청을 최근 100건까지 보관하고, 발행된 결과 장비의 이미지·성능·설명·태그·비용·공정·투입 재료·장비 액션을 읽기 전용 스냅샷으로 표시한다.
- 개조 계통과 투입 재료의 강제 연결을 제거해 운영자가 두 항목을 독립적으로 선택할 수 있게 했다.
- 검증: 공방 집중 테스트 40건, 변경 파일 ESLint, 2056×1147 및 390×844 인증 화면에서 완료 결과 스냅샷·반응형·콘솔 오류 0건 확인, critical risk review
- 관련 커밋: `7f77546`
- 잔여 확인: 실제 Mongo 품목 조회를 포함한 상이한 개조 계통·특수 재료 조합의 mutation 통합 테스트는 라이브 데이터 보호를 위해 실행하지 않았다.

## 2026-08-06 · 기능 확장 · 구조화 장비 액션과 재료 범위

- 개인·공용 인벤토리 재료를 견적에서 구분하고, 결과 장비에 복수 액션·액션별 충전·일반 탄약·거치 규칙을 구조화해 편집하도록 확장했다.
- 결과 이미지와 원본 장비 이미지를 별도 역할로 유지하며, 충전 상태가 있는 장비 stack은 수량 1일 때만 escrow하도록 닫았다.
- 검증: 공방·VTT 포함 집중 테스트 74건 중 68건 통과·Mongo 통합 6건 환경 부재로 skip, `pnpm typecheck`, `pnpm lint`, `pnpm build`, 인증 브라우저 데스크톱 확인
- 관련 커밋: `30ffe959`
- 후속 작업: 네베드용 실제 프리셋·견적·수락·납품은 확정 스펙과 별도 라이브 승인 전까지 생성하지 않았다.

## 2026-08-06 · 기능 확장 · 네베드 전용 청사진

- `CMMG Mk.47 Mutant 「피안의 보루」` 프리셋을 1,200 CR, 180분, TOWASKI 화기 개조와 VERNIER 포스코어 통합 검수의 복합 공정으로 확정했다.
- 결과 장비에 12발 탄창, 액션 1 거치·해제, 거치 중 이동 제한, DIAMOND 대각선 사격, CENSOR-3 1발 기반 고정 30 심령 추가 피해를 보존한다.
- 승인된 결과 이미지와 원본 장비 이미지를 분리하고, seed와 migration은 기본 dry-run 및 수량 drift 차단으로 운영 쓰기를 fail-closed 처리한다.
- 검증: 공방·VTT·migration 집중 테스트 60건 중 58건 통과·Mongo 통합 2건 skip, `pnpm typecheck`, `pnpm lint`, `pnpm build`, seed payload read-only dry-run, critical risk review
- 관련 커밋: `44b83580`, `0ca9972c`, `cfb2df1`
- 운영 경계: 청사진 seed는 `equipment_workshop_blueprints`의 `DRAFT` 문서만 만들며 완성품 카탈로그·요청 상태·재료·크레딧·인벤토리를 변경하지 않는다. 깨진 음절 차감과 CENSOR-3 지급, 견적·수락·제작·납품은 공방 운영자가 화면의 정상 흐름에서 처리하고 별도 migration으로 선행하지 않는다.

## 2026-08-07 · 기능 변경 · 조건부 관료 표결 제작 연동

- 제작시간 하한을 1일(1,440분)로 바로잡고, 공방 운영자가 견적마다 1~30일 범위의 시간을 직접 설정하도록 변경했다.
- 견적에 관료 표결 제목·내용, 조건부 개인·공용 재료, 가결 산출물을 임의로 구성할 수 있게 해 특정 재료나 캐릭터에 종속되지 않는 구조로 일반화했다.
- 견적 수락 시 연결 표결 원장만 자동 생성하고 조건부 재료는 예약·차감하지 않는다. 표결 가결 후 완료품 수령 시점에만 재고를 다시 확인해 조건부 재료 차감과 산출물 지급을 한 트랜잭션으로 처리한다.
- 부결이면 기본 결과 장비만 지급하며, 가결 후 재고가 부족하면 기본 장비 수령까지 전부 롤백해 재고 보충 뒤 같은 요청을 다시 수령할 수 있다.
- 검증: 공방·표결·seed 집중 테스트 72건, 웹 typecheck·lint·production build, REGISTRAR 테스트 23건·build, 인증된 로컬 관리자 화면에서 1일 하한·커스텀 시간·조건부 편집 UI와 콘솔 오류 0건 확인
- 관련 커밋: `c1dc0e5a`
- 운영 경계: 라이브 견적 발행·수락·표결 생성·재료 차감·완성품 수령은 실행하지 않았다.

## 2026-08-07 · 운영 반영 · 피안의 보루 v1 청사진

- `CMMG Mk.47 Mutant 「피안의 보루」` v1을 운영 DB의 `equipment_workshop_blueprints`에 `DRAFT` 1건으로 적재해 공방 운영 설계안 선택기에 연결했다.
- 최종 검토와 완료 결과 보관 화면이 복수 `equipmentActions`를 읽도록 보강해 U1 `총기 거치 전환`과 U2 `파쇄음절탄 사격`을 더 이상 `없음`으로 표시하지 않는다.
- 검증: 운영 DB 재조회에서 청사진 1건·연결 요청 0건·연결 표결 0건, 공방 집중 테스트 51건, `pnpm typecheck`, `pnpm lint`, 인증 브라우저 데스크톱·390×844에서 U1/U2 표시·가로 넘침 없음·콘솔 error/warning 0건 확인
- 관련 커밋: `c1dc0e5a`, `ff61e4ac`
- 운영 경계: 청사진 선택만 읽기 전용으로 확인했으며 견적 발행·수락·제작·납품·수령은 실행하지 않았다.

## 2026-08-07 · 버그 수정 · 프리셋 장비 액션 상세 표시

- 복수 액션 프리셋을 선택했을 때 비어 있는 단일 액션 입력칸 대신 U 액션별 상세 카드를 표시하도록 바로잡았다.
- `피안의 보루`의 U1 `총기 거치 전환`과 U2 `파쇄음절탄 사격`에 액션 비용, 거치 조건, 사거리, 기본 사격 병행, 일반 탄약 미소모, CENSOR-3 소모, 고정 30 심령·방어 무시 피해를 명시한다.
- 검증: 공방 집중 테스트 51건, `npx tsc --noEmit`, `npm run lint`, `npm run build`, 인증된 로컬 관리자 화면 1280×720에서 프리셋 선택·U1/U2 카드·최종 검토 요약·가로 넘침 없음·콘솔 오류 0건 확인
- 관련 커밋: `7ed6e5c3`
- 운영 경계: 프리셋을 편집본에 불러오는 읽기 전용 확인만 수행했으며 견적 발행·검토 시작·수락·제작·납품·수령은 실행하지 않았다.

## 2026-08-07 · 버그 수정 · 장비 액션 편집 UI와 검증

- 복수 액션 카드를 내용 높이에 맞는 반응형 그리드로 재구성해 짧은 U1 카드가 긴 U2 카드 높이까지 늘어나던 문제를 제거하고, 액션 코드·효과·전투 규칙의 시각적 위계를 정리했다.
- 프리셋에서 불러온 액션의 코드·이름·설명·효과·사거리·피해·충전 수치를 요청 편집본에서 개별 수정하고 설계안·견적 결과로 저장할 수 있게 했다. 재고 차감과 결합된 VTT 실행 계약은 읽기 전용으로 유지한다.
- 기본 사격 연동 액션의 사거리가 무기 전투 프로필을 벗어나지 않도록 클라이언트와 서버 양쪽에서 검증하고, 고정 운영 안내가 하단 조작부를 가리지 않도록 닫기 동작을 추가했다.
- 검증: 공방 집중 테스트 52건, `npm run typecheck`, `npm run lint`, `npm run build`, 인증된 로컬 관리자 화면 1280×720에서 U1/U2 카드 높이·개별 편집 반영·프리셋 재적용 초기화·가로 넘침 없음·콘솔 오류 0건 확인
- 관련 커밋: `a2923175`
- 운영 경계: 프리셋 불러오기와 로컬 편집·초기화만 확인했으며 견적 발행·검토 시작·수락·제작·납품·수령은 실행하지 않았다.

## 2026-08-07 · 밸런스 변경 · 피안의 보루 소리 피해

- U2 `파쇄음절탄 사격`을 고정 30 심령 피해에서 방어를 무시하는 고정 15 소리 피해로 변경하고, 대상 SAN을 추가 감소시키는 규칙을 프리셋·청사진·소모품·VTT 전달 계약에 동기화했다.
- U1 `총기 거치 전환`은 거치와 해제에 각각 액션 1을 소모하고, 거치 중 이동 불가·다이아몬드 대각선 사격·훈련장 범위 참조가 한 흐름으로 읽히도록 정리했다.
- 신규 청사진은 v2로 생성하고, 기존 운영 `DRAFT` v1만 갱신하는 CAS payload에 U1/U2 효과·방어 무시·표결 본문까지 확인하는 완료 조건을 추가했다.
- 검증: 관련 계약 테스트 72건, `npm run typecheck`, `npm run lint`, `npm run build`, seed payload 운영 DB 읽기 전용 dry-run, 인증된 로컬 관리자 화면 1280px에서 프리셋 편집 카드·가로 넘침 없음·콘솔 오류 0건 확인, critical risk review
- 관련 커밋: `9fef5d8b`
- 운영 경계: 라이브 청사진·CENSOR-3 마스터 품목 갱신, 견적 발행·수락·표결 생성·재료 차감·제작·수령은 실행하지 않았다.

## 2026-08-07 · 규칙 정정 및 운영 반영 · CENSOR-3 SAN 직접 감소

- U2 `파쇄음절탄 사격`의 `SOUND 15`를 HP에 더하는 추가 피해가 아니라, 대상의 방어 수단과 DEF를 무시하고 SAN을 고정 15 감소시키는 별도 판정으로 정정했다.
- 한 번의 사격에서 네베드의 소총 패시브가 반영된 거리별 기본 물리 피해 27/32/32는 정상 물리 피해로 처리하고, 이어지는 SAN 15 감소만 방어를 무시한다. 액션 1과 CENSOR-3 1발을 소모하며 일반 탄약은 소모하지 않는다.
- 공방 관리자 화면은 `SOUND`를 `SAN 감소`와 `추가 판정`으로 표시하고, Nochichim 전투 엔진은 해당 값을 HP가 아닌 SAN에 직접 적용한다.
- 운영 DB의 CENSOR-3 마스터 품목 `_id=6a7588b37c80e35d939dffc7`과 `피안의 보루` 청사진 `_id=6a7588d07c80e35d939dffca`를 승인된 내용으로 갱신했다. 청사진은 `v2 / DRAFT / 1,200 CR / 1,440분`이며 비공개·판매 불가 정책은 유지했다.
- 검증: StarGate 관련 계약 테스트 63건, `npm run typecheck`, `npm run lint`, Nochichim 관련 회귀 테스트 3종, 운영 DB 쓰기 후 원문 재조회 및 연결 공방 요청·v2 표결·캐릭터/공용 CENSOR-3 재고 각 0건 확인, critical risk review
- 관련 커밋: `a698e662`, Nochichim `5c02fca`
- 운영 경계: 마스터 품목과 `DRAFT` 청사진만 갱신했다. 견적 발행·수락·표결 생성·재료/크레딧 차감·제작·납품·수령은 실행하지 않았다.
