# /erp/gallery

## 2026-08-17 · 기능 추가 · 아카이브 그리드 masonry 전환

- 세로 도판이 섞여 그리드 행마다 가장 큰 카드 높이에 맞춰지면서 카드 하나당 최대 417px 씩 빈 공간이 생겼다. `grid-auto-rows: 4px` 위에서 카드마다 실제 높이만큼 row 를 점유하게 해 빈 공간을 없앴다.
- CSS `columns` 는 쓰지 않았다. `columns` 는 전체 높이를 열 수로 나눠 담기 때문에 424장 규모에서는 1열에 앞쪽 1/3 이 전부 들어가 좌우로 읽을 수 없게 된다. grid row span 방식은 문서 순서를 유지한다.
- 카드 높이는 `ResizeObserver` 로 측정해 `--card-span` 으로 넣는다(`useMasonrySpan`). `align-self: start` 라 카드 높이가 점유 row 수와 무관해 되먹임이 생기지 않는다. `grid-auto-rows` 4px · `margin-bottom` 14px 는 [GalleryClient.tsx](app/(erp)/erp/gallery/GalleryClient.tsx) 의 `GRID_ROW`/`GRID_GAP` 과 값이 같아야 한다.
- masonry 는 실제 높이가 필요하므로 그리드 카드에서 `content-visibility: auto` 를 해제했다. 화면 밖 카드가 `contain-intrinsic-size` 플레이스홀더 높이를 보고해 열 배치가 어긋난다. 이미지는 여전히 `loading="lazy"` 라 네트워크 비용은 그대로다.
- 검증: 1440px 3열 · 900px 2열 · 390px 1열에서 카드 겹침 0건, 카드 간 수직 간격 14~18px(4px row 반올림 오차 최대 +4px), 가로 overflow 0건. 갤러리 테스트 41/41, `pnpm typecheck`, 전체 `pnpm lint`, `pnpm assets:audit`
- 관련 커밋: `76a6ca47`
- 후속 작업: 그리드 카드가 전량 렌더되므로 항목이 1000장 규모로 늘면 가상 스크롤을 검토한다.

## 2026-08-17 · 버그 수정 · 중복 도판 제거와 전 구간 레이아웃 점검

- 지각 해시(16×16 정규화 서명 + 비율 일치)로 세션 도판 전체를 비교해 중복 컷씬 14장을 제거했다. 컷씬은 재인코딩 사본이 섞여 있어 바이트 해시로는 안 잡힌다. 최종 컷씬 424장.
- 본문 도판은 MongoDB 보고서 본문이 참조하므로 제거 대상에서 제외했다. 본문끼리 중복인 `neved/memory-dive-chamber`↔`memory-dive-return`, `s1e4-prato/inverted-sock-*` 는 보고서 내용 문제라 그대로 뒀다.
- 동일 프레임이 아닌 두 그룹은 육안 확인 후 보류했다 — `hwayangyeonhwa` 노트 4장(적힌 글자가 다름), `mini03-5959` 관제실 3장(인물 유무가 다름).
- 오버레이 메타데이터를 큰 히어로 카드 전용으로 좁히고, 보조 카드도 아카이브 그리드와 같은 "이미지 + 아래 캡션" 형태로 통일했다. 보조 카드에서 세로 도판이 높이 상한에 걸려 letterbox 가 생기던 문제도 함께 해소됐다.
- 높이 상한을 올렸다(히어로 `min(76vh, 720px)` / 보조·그리드 `800px`, 900px 이하 `74vh`). 1920px 에서 5%, 900px 에서 21% 발생하던 letterbox 가 사라졌다.
- 검증: 실제 매니페스트 도판으로 툴바·필터·showcase·그리드·상세보기·업로드 모달을 함께 렌더해 390/768/900/1024/1440/1920px 점검. 전 구간에서 가로 overflow 0건 · 본문↔이미지 겹침 0건 · 카드 밖 넘침 0건 · `CURRENT EXHIBIT` 라벨과 `SESSION` 칩 충돌 0건 · 직접 텍스트 14px 미만 0건 · 최대 letterbox 0%. 갤러리 테스트 41/41, `pnpm typecheck`, 전체 `pnpm lint`, `pnpm assets:audit`, `git diff --check`
- 측정 주의: `.card` 의 `content-visibility: auto` 때문에 화면 밖 카드는 `contain-intrinsic-size` 플레이스홀더 크기를 보고한다. 레이아웃 계측 시 이 속성을 임시로 끄고 재야 한다.
- 관련 커밋: `4c41d99d`
- 후속 작업: 세로 도판이 많아 그리드 행 높이 편차가 크다. 여백이 거슬리면 CSS `columns` masonry 로 전환할 수 있다(`content-visibility` 와의 상성 확인 필요).

## 2026-08-17 · 버그 수정 · 합성 컷씬 분할, 앨범 미연결 세션 수록, 반응형

- 컷씬 매니페스트 키를 자산 폴더에서 `sessionId` 로 바꿨다. 폴더 추론이 보고서 본문 도판에 의존해, 본문 도판이 하나도 없는 5개 세션(질서·질서 미니·송사리·망령·5959사태)에는 컷씬이 붙지 않았다. 이 세션들에 폴더를 신설해 수록했다(9·11·19·42·27장). 프라토 1부/2부처럼 폴더를 공유하는 세션도 `sessionId` 로 갈라 각자 앨범에 붙는다.
- 두 컷이 한 파일에 붙어 있던 합성 도판 4장을 개별 컷으로 분할했다. 인접 라인 밝기 불연속으로 후보를 뽑고(단일 이음선 + 기준 대비 15배 이상) 9장을 전량 육안 확인해 4장만 확정했다. 나머지는 어두운 장면·흰 배경 오검출이다.
- 세션 귀속이 바뀐 폴더에서 파일 이름을 재사용하면 배정과 내용이 어긋나므로 컷씬 자산을 전량 다시 내려받았다. 최종 438장.
- 반응형: 아카이브 그리드는 항상, 히어로/보조 카드는 900px 이하에서 메타데이터를 이미지 위 오버레이 대신 아래 캡션 블록으로 둔다. 카드가 작아질수록 오버레이가 도판을 최대 80%까지 덮었다. 모바일에서 `CURRENT EXHIBIT` 라벨이 `SESSION` 칩과 겹치던 것도 라벨을 카드 위로 빼서 해결했다.
- 그리드 카드 비율 하한을 0.75 로 올려 세로 도판 letterbox 를 없애고, `contain-intrinsic-size` 를 `auto` 로 바꿔 스크롤 튐을 줄였다.
- 검증: 매니페스트 438장 ↔ 파일 1:1 일치(고아 0), 렌더 하네스에서 390/768/1440px 가로 overflow 0건·본문과 이미지 겹침 0건·표시 비율 원본 일치, 갤러리 테스트 41/41, `pnpm typecheck`, 전체 `pnpm lint`, `pnpm assets:audit`
- 관련 커밋: `497a05ed`
- 후속 작업: 세션 귀속은 로그 채널 마커 기준이라 경계(프라토 1/2부, 선택/송사리)에서 어긋날 수 있다. `역할극`·`변곡점 2부` 세션 몫 56장은 보고서가 없어 여전히 제외 상태다. 동일 원본이 두 세션에 중복 수록된 사례(`romantid` ↔ `s1e5-sector-c`)가 있어 내용 해시 기반 중복 제거가 필요하다.

## 2026-08-17 · 기능 추가 · 미수록 세션 컷씬 도판 수록

- 인게임-컷씬 채널 543장 중 저장소에 반영된 것은 53장뿐이었다. 앨범을 특정할 수 있는 358장을 세션 폴더에 추가해 갤러리 수록 대상을 74장에서 432장으로 늘렸다.
- 세션 귀속은 로그 채널(채널 ID `1482924316067762216`)의 세션 마커 타임스탬프와 대조해 정했다. 확신 매칭이 있는 메시지는 그 세션으로 고정(143장), 나머지는 가장 가까운 마커로 추정(215장)했다. 기존 확정 매칭 45건으로 검증했을 때 41건 적중(91%)이라 part1/part2 경계 등에서 앨범 링크가 어긋날 수 있다.
- 갤러리 피드는 보고서 본문 마크다운 이미지만 수집하므로 파일 추가만으로는 노출되지 않는다. 보고서가 참조하는 자산 폴더를 키로 `SESSION_CUTSCENES` 를 이어 붙이는 경로를 [feed.ts](lib/gallery/feed.ts) 에 신설했다. 보고서 본문(MongoDB)은 건드리지 않는다.
- 컷씬 도판은 매니페스트에 width/height 를 담아 카드 비율이 로드 전에 확정된다. 본문 도판은 여전히 `null` 이라 로드 후 확정된다.
- 원본은 크롭 없이 `fit: inside` 장변 1400 상한 + webp(q78) 로 재인코딩했다. `public/assets/session-reports` 가 14MB 에서 60MB 로 늘었다.
- 보고서가 없어 갤러리 앨범이 없는 세션(질서·송사리·망령·역할극·변곡점 2부) 몫 132장은 귀속 대상이 없어 제외했다.
- 검증: 매니페스트 358항목 ↔ 파일 1:1 일치, 갤러리 테스트 40/40(컷씬 수록·미수록 경로 2건 추가), `pnpm typecheck`, 전체 `pnpm lint`, `pnpm assets:audit`
- 관련 커밋: `e5e544f3`
- 후속 작업: 컷씬 도판은 캡션이 없어 카드 제목이 `<세션명> 컷신 NN` 이고 설명이 비어 있다. 앨범 없는 132장은 해당 세션 보고서가 생기면 같은 경로로 수록할 수 있다.

## 2026-08-17 · 버그 수정 · 이미지 잘림과 라벨 가독성

- 카드 프레임을 이미지 실측 비율에 맞추고 `object-fit` 을 cover 에서 contain 으로 바꿔 카드에서 이미지가 잘려나가던 동작을 없앴다. 팬아트는 저장된 width/height 를, 세션 도판은 로드 시 naturalWidth/naturalHeight 를 측정해 `--card-ratio` 로 반영한다.
- `aspect-ratio` 가 가로 축까지 축소시켜 세로 이미지 카드가 열 너비보다 좁아지던 문제를 `width: 100%` 로 고정했다. showcase 보조 카드도 비율 기반으로 바꿔 letterbox 와 blur 백드롭 레이어를 함께 제거했고, 아카이브 그리드는 초광각 도판이 메타데이터 오버레이에 눌리지 않도록 4열에서 3열로 줄였다.
- 상세 보기 스테이지의 고정 높이를 걷어내고 이미지 비율이 높이를 결정하게 해, 초광각 도판 위아래로 424px 생기던 빈 공간을 52px(패딩)로 줄이고 액자처럼 보이던 box-shadow 를 제거했다.
- 이 모듈만 참조하던 미정의 토큰(`--paper-1/2/3`, `--line-2`)을 실제 토큰으로 교체했다. 무효 선언 탓에 SESSION 라벨 색, 카드 테두리, 모달 배경, select/textarea 테두리가 전혀 적용되지 않고 있었다. 사진 위 라벨은 칩 배경 불투명도·gold-dim 테두리·text-shadow·태그 칩 배경으로 대비를 높였다.
- 보고서 도판 74장 중 60장이 1035x503 으로 일괄 크롭돼 인물 머리와 화면 양끝이 잘려 있던 것을 확인하고, 디스코드 인게임-컷씬 채널 원본 553장과 대조해 41장을 크롭 이전 화면으로 되돌렸다. 원본을 특정하지 못한 17장, 혈흔 후처리가 들어간 1장, 원본이 더 작은 1장은 그대로 두었다.
- 검증: 실제 CSS·실제 도판 렌더 하네스에서 1440/900/600px 표시 비율이 원본과 일치(2.058/1.792/1.333 오차 0)·가로 overflow 0건, 교체 자산 41장 before/after 육안 전수 검수, 갤러리 테스트 38/38, `pnpm typecheck`, 전체 `pnpm lint`, `pnpm assets:audit`, `git diff --check`
- 관련 커밋: `2aed5e65`, `60b7590a`
- 후속 작업: 원본을 특정하지 못한 17장은 채널 원본 유무 확인이 필요하다. 세션 도판은 피드에서 width/height 가 `null` 이라 카드 비율이 로드 후 확정되므로, 자산 치수 매니페스트를 도입하면 초기 레이아웃 이동을 없앨 수 있다.

## 2026-08-17 · 기능 추가 · 세션 앨범과 회원 팬아트

- 접근 가능한 세션 보고서의 도판을 앨범으로 자동 구성하고 회원 팬아트를 검색·종류·앨범 필터와 반응형 4/2/1열 그리드로 함께 탐색할 수 있게 했다.
- 키보드 이동·focus 복원·scroll lock을 갖춘 lightbox와 업로드·메타데이터 수정·소유자 삭제·V 이상 운영 숨김 UI를 추가했다.
- 팬아트 원본과 thumbnail을 private Blob으로 분리하고, MIME·decode·6MP·4MB 검증, 사용자별 직렬 lease, 일일 원자적 quota, idempotency·CAS, durable cleanup과 보고서 연결 race 방어를 적용했다.
- 보고서와 팬아트 realtime resource를 갤러리 Query에 연결하고, 연결 장애일 때만 60초 polling으로 복구하도록 했다.
- 운영 기본 잠금은 유지했으며 라이브 Blob·DB index·페이지 잠금 상태는 변경하지 않았다.
- 검증: 갤러리 행동·계약 테스트 37/37, web realtime 10/10, core 계약 2/2, worker realtime 6/6, `pnpm typecheck`, 전체 `pnpm lint`, 102 route production build, 인증 로컬 1440/900/600px의 4/2/1열·검색·필터·lightbox 키보드/focus·가로 overflow·console 오류 0건, critical risk review
- 관련 커밋: `11fc6e98`
- 후속 작업: 운영 활성화 전 승인된 환경에서 private Blob 토큰과 갤러리 index를 적용하고 core/worker를 web보다 먼저 배포한다. 기존 `/assets/session-reports/**` 도판의 byte-level 비공개화는 자산 이전 정책과 함께 별도 진행한다.

## 2026-08-17 · UI 개편 · 시네마틱 전시와 상세 보기

- 검색·필터 결과의 첫 작품을 `CURRENT EXHIBIT`로 크게 배치하고 다음 두 작품을 보조 전시, 나머지를 4/2/1열 아카이브로 구성해 갤러리의 시각적 위계를 강화했다.
- 카드의 흰 문서 패널을 없애고 이미지 위에 다크 HUD, 그라데이션, 미세한 스캔라인과 메타데이터를 겹쳐 NOVUS ERP의 기록 보관소 분위기로 통일했다.
- 상세 보기를 대형 다크 이미지 스테이지와 메타 레일로 재설계하고, 모바일에서는 이미지와 정보가 자연스럽게 이어지는 전체 화면 구조로 전환했다.
- 검색 결과가 한 작품일 때 탐색 버튼을 숨기고, 기존 화살표 키·Escape·focus trap·focus 복원·scroll lock과 팬아트 편집·관리·삭제 동작을 유지했다.
- 검증: `pnpm typecheck`, 전체 `pnpm lint`, `git diff --check`, 인증 로컬 1440/900/600px의 전시·4/2/1열·단일/2개 검색 결과·상세 화면·키보드 탐색·focus 복원·가로 overflow·깨진 이미지·console 오류 0건, 읽기 전용 UI 리뷰
- 관련 커밋: `19905785`

## 2026-08-17 · 버그 수정 · 세션 도판 태그 규격화

- 보고서 전체 참가자 목록을 모든 세션 도판의 이미지 태그로 복사하던 동작을 제거했다. 참가자는 이미지의 피사체나 작가를 뜻하지 않으므로 카드·상세·검색 메타데이터로 사용하지 않는다.
- 세션 도판의 자동 태그는 `세션 / 메인·미니 / 보고서 번호` 세 항목으로 고정하고, 메인·미니 구분은 기존 작전 보고서 번호 계산 결과의 `series` 값을 그대로 사용한다.
- 갤러리 전용 보고서 타입과 MongoDB projection에서도 `participants`를 제외해 불필요한 참가자 데이터가 피드 생성 경계로 들어오지 않게 했다. 팬아트의 사용자가 입력한 자유 태그 규칙은 변경하지 않았다.
- 검증: 갤러리 테스트 38/38, `pnpm typecheck`, 전체 `pnpm lint`, scoped ESLint, `git diff --check`, 인증 로컬에서 MINI06 카드·상세 `세션 / 미니 / MINI06`, 참가자 전체 문자열 검색 0건, MINI06 검색 12건, 메인 검색 42건, 가로 overflow·console 오류 0건, 읽기 전용 코드 리뷰
- 관련 커밋: `3486a8c5`

## 2026-08-17 · UI 개편 · 편집형 작품 상세 뷰어

- MoMA·Artsy·Magnum·Gagosian의 작품 상세 화면에서 공통으로 보이는 작품 우선 배치, 절제된 편집형 캡션, 최소한의 화면 chrome을 참고해 상세 보기를 다시 구성했다.
- 이미지 스테이지와 메타데이터 패널 사이의 경계, 사각형 이전·다음 버튼, 하단 중복 내비게이션, 태그 칩을 제거하고 하나의 다크 캔버스와 반투명 원형 컨트롤로 통합했다.
- 제목과 설명이 같은 세션 도판은 설명을 반복하지 않고, 세션 번호·작가·작전 보고서 링크·태그를 본문 타이포 위계로 정리해 ERP 관리 패널 인상을 줄였다.
- 920px 이하에서는 이미지 무대 아래 정보 시트가 겹쳐지는 단일열 레이아웃으로, 760px 이하에서는 safe area를 고려한 전체 화면 감상 구조로 전환했다.
- 기존 화살표 키·Escape·focus trap·focus 복원·overlay 닫기·scroll lock과 단일 검색 결과의 탐색 버튼 숨김 동작을 유지했다.
- 검증: 갤러리 테스트 38/38, `pnpm typecheck`, 전체 `pnpm lint`, `git diff --check`, 인증 로컬 1440/900/600px의 상세 화면·키보드 탐색·focus 복원·가로 overflow·깨진 이미지·console 오류 0건, 읽기 전용 UI 리뷰
- 관련 커밋: `b673150c`
