# /erp/characters

## 2026-08-04 · 버그 수정 · 최종 능력치 표시

- 캐릭터 카드의 HP와 SAN이 기본값만 표시하던 문제를 고쳐 포인트 보정값(Δ)을 합산한 최종 수치를 표시한다.
- 같은 최종값 기준으로 기본 공격력(ATK)과 방어력(DEF)을 카드에 추가했다.
- 검증: 캐릭터 카드 능력치 집중 테스트 3건, shared-db 빌드, `pnpm typecheck`, `pnpm lint`, 인증된 로컬 1440px·390px 화면 확인
- 관련 커밋: `583bd5f4`

## 2026-08-06 · 데이터 입력 개선 · 문학·철학 통합

- 신규 생성·수정에서 정확한 `철학` 기술 훈련 표기를 `문학`으로 통합하고 중복을 제거한다.
- 기존 레코드에 문학과 철학이 따로 공존하면 자동 합치지 않고 dry-run 수동 판단 대상으로 남긴다.
- 검증: 입력·API·migration 집중 테스트, `pnpm typecheck`, `pnpm lint`, `pnpm build`
- 관련 커밋: `30ffe959`
- 후속 작업: 읽기 전용 dry-run만 수행했으며 라이브 migration은 실행하지 않았다.
