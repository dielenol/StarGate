# /erp/characters

## 2026-07-02 · UI·표시 규칙 개선

- 캐릭터별 패턴과 상태 표현을 카드에 적용하고 GM 테스트 캐릭터 숨김 토글을 추가했다.
- 표시명·역할·소속을 공용 규칙으로 통일하고 등급·직군 배지 판별을 보강했다.
- 검증: 목록 projection, 표시 포매터와 카드 렌더링 변경을 연결 커밋별로 대조했다.
- 관련 커밋: `544f03e`, `2f29cb9`, `00b8c83`, `2606a6e`, `e196d7f`, `c7e0539`

## 2026-08-04 · 버그 수정 · 최종 능력치 표시

- 캐릭터 카드의 HP와 SAN이 기본값만 표시하던 문제를 고쳐 포인트 보정값(Δ)을 합산한 최종 수치를 표시한다.
- 같은 최종값 기준으로 기본 공격력(ATK)과 방어력(DEF)을 카드에 추가했다.
- 검증: 캐릭터 카드 능력치 집중 테스트 3건, shared-db 빌드, `pnpm typecheck`, `pnpm lint`, 인증된 로컬 1440px·390px 화면 확인
- 관련 커밋: `583bd5f4`

## 2026-08-06 · 데이터 입력 개선 · 문학·철학 통합

- 신규 생성·수정에서 정확한 `철학` 기술 훈련 표기를 `문학`으로 통합하고 중복을 제거한다.
- 기존 레코드에 문학과 철학이 따로 공존하면 자동 합치지 않고 dry-run 수동 판단 대상으로 남긴다.
- 검증: 입력·API·migration 집중 테스트, `pnpm typecheck`, `pnpm lint`, `pnpm build`
- 관련 커밋: `30ffe959`
- 후속 작업: 읽기 전용 dry-run만 수행했으며 라이브 migration은 실행하지 않았다.

## 2026-08-10 · 기능 추가 · 게스트 공개 캐릭터 열람

- 게스트는 공개 캐릭터 목록과 상세를 `U` 등급 기준으로 열람할 수 있으며, 등급이 부족한 능력치·어빌리티·로어는 분류 처리한다.
- 소유자 ID, 필드별 등급 override, 원문 로어와 개인 인벤토리는 게스트 응답에서 제거하고 장비 관리 권한은 항상 비활성화한다.
- 검증: 게스트 접근 계약 테스트 11/11, 실제 게스트 API에서 소유자·override·원문 비노출 확인, 데스크톱 목록·상세 및 390×844 화면 확인, `pnpm typecheck`, `pnpm lint`, `pnpm build`, critical risk review
- 관련 커밋: `aa3ce2d8`

## 2026-08-10 · 보안 개선 · 캐릭터 카드 운영 메타 제거

- 서버 마스킹 계산에는 캐릭터별 등급 override를 사용하되, 게스트와 일반 회원의 API·RSC 카드 DTO에서는 해당 운영 맵을 항상 제거한다.
- 게스트는 소유자 ID 없이 `U` 등급으로 투영된 공개 카드만 받고, 기존 회원의 공개/비공개 필터와 표시 결과는 유지한다.
- 검증: 회원·게스트 DTO 테스트, 실제 게스트 19개 카드의 override 0건과 능력치 마스킹, 데스크톱 목록 가로 넘침·콘솔 오류 0건, `pnpm typecheck`, `pnpm lint`, `pnpm build`, critical risk review
- 관련 커밋: `2df03010`

## 2026-08-13 · 성능 최적화 · 화면 밖 카드 렌더 지연

- 캐릭터 카드와 검색·필터 결과는 DOM 순서 그대로 유지하고, 화면 밖 카드의 최초 layout·paint만 브라우저가 지연하도록 했다.
- 검증: JTEST 목록 19건의 첫·마지막 카드 `content-visibility: auto`와 끝 스크롤 렌더, 집중 테스트·타입 검사·lint·production build
- 관련 커밋: `76845d44`

## 2026-08-30 · UI 개선 · 캐릭터 헤럴딕 식별자

- 사이드바·검색 내비게이션의 캐릭터 항목과 목록의 MAIN/MINI, 실험체·관료·군인·과학자 배지를 로어와 실제 분류 의미에 맞춘 단색 헤럴딕 SVG로 교체했다.
- 기존 분류·필터·능력치·권한은 유지하고, MAIN/MINI는 신원조회와 동일한 OrgIcon source를 사용한다.
- 검증: 16·20·24·32px 밝은/어두운 배경 QA, 동기화·조직·자산 테스트 51건, `pnpm typecheck`, `pnpm lint`, 인증된 로컬 1440×1000·390×844 목록과 가로 넘침·깨진 이미지·브라우저 오류 0건
- 관련 구현 커밋: `27173b41`
