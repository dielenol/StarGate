# ERP 공용 셸

## 2026-06-30 · 헤더 알림 동작 개선

- 알림 드롭다운을 명시 상태로 전환해 바깥 클릭, Escape와 링크 이동 시 닫히도록 했다.
- 읽지 않은 알림 전체 읽기와 처리 중·비활성 상태를 헤더 액션에 연결했다.
- 검증: ERPHeader의 이벤트·mutation 연결을 커밋 diff로 확인했다.
- 관련 커밋: `4d3124f`, `298cb5a`, `56112a5`

## 2026-07-01 · 내부 이동 피드백

- 공용 Button의 내부 경로를 Next Link로 렌더링해 페이지 이동 중 pending 표시가 동작하도록 했다.
- 검증: 공용 Button의 내부·외부 링크 분기와 NavPending 연결을 확인했다.
- 관련 커밋: `e44ba15`

## 2026-07-13 · hydration 안정화

- 확장 프로그램의 초기 style 주입 감시 시점과 종료 범위를 제한했다.
- 알림 DTO, 크레딧 정렬과 보고서 CSS 계산을 결정적으로 만들어 ERP hydration 오류를 수정했다.
- 검증: 루트 layout과 영향 화면의 서버·클라이언트 값 직렬화 변경을 대조했다.
- 관련 커밋: `5dd44a7`, `6bd07d8`

## 2026-07-19 · BGM 확장

- 기존 랜덤 재생 목록에 `NOVUS 08` 음원을 추가했다.
- 검증: ERPHeader 트랙 목록과 정적 음원 경로를 확인했다.
- 관련 커밋: `b9f959f`

## 2026-09-08 · UI 개선 · 헤더와 사이드바 탐색

- 헤더의 브랜드·페이지 문맥·도구 간격을 정리하고, BGM·검색·알림 버튼을 부드러운 라운드와 일관된 정렬로 맞췄다. 검색 문자를 기존 SVG 아이콘으로 교체하고 알림 목록은 화면 높이에 맞춰 스크롤되며 Escape로 닫으면 알림 버튼에 포커스가 돌아온다.
- 사이드바는 그룹과 하위 메뉴 간격·활성 표시를 정리하고 현재 경로 하나에 `aria-current="page"`를 제공한다. 메뉴 본문만 스크롤되어 메인 사이트 복귀 영역이 유지된다.
- 모바일 메뉴는 100dvh와 안전 영역을 반영하고 닫기·복귀 영역을 고정했다. 운영 잠금 도구 위에 표시되며 기존 Escape·Tab 순환·배경 클릭·이동 후 닫힘을 유지하고 모션 감소 설정을 따른다.
- 검증: `pnpm typecheck`, `pnpm lint`, 읽기 전용 독립 리뷰, 인증 로컬 GM 1440·1280·1279·834·390·320px에서 가로 넘침·검색·알림·메뉴 탐색, 834×500에서 고정 버튼·BGM 패널, 중첩 메뉴의 단일 현재 경로, 플레이어 계정 390px에서 GM 메뉴 비노출과 선택 표시를 확인했다. BGM 재생·정지·볼륨, 브라우저 오류 0건, 관찰한 GM 업무 mutation 0건을 확인했다.
- 관련 구현 커밋: `35aedf9b`
- 운영 경계: 헤더·사이드바 4파일만 변경했다. 하위 페이지 본문·인증·권한·메뉴 경로·DB·Query·BGM 재생 엔진은 유지했으며 push·배포는 하지 않았다. UI 시안을 되돌릴 때 구현 커밋만 revert할 수 있다.
