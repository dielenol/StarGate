# 시즌 1 즉시 작업 구현·근거 원장

## 구현 범위

- 아르고 등록 준비, 네베드 전용무기 후보, ZULU-0028 연구소 v1, 미스터비스트 소다 불량 판정, 문학·철학 입력 통합, Nochichim 계약 보강을 다룬다.
- 시즌 2와 9화 아이디어, 이미 완료된 이동식 클레이모어 재구현, 라이브 DB·Discord·배포 mutation은 제외한다.
- 코드와 테스트, dry-run 페이로드까지만 구현한다. 운영 재료·크레딧·인벤토리·장비 상태는 별도 승인 전까지 바꾸지 않는다.

## 근거 정책

1. 현재 사용자 지시, 첨부 원문, 같은 대상의 현재 DB·저장소 자료, 대상이 명시된 과거 대화·첨부 순으로 사용한다.
2. 대화 제목, AI 답변, 유사 이름·이미지·파일명, 주변 정황은 단독 근거로 사용하지 않는다.
3. 원본 장비와 개조 결과 이미지, 캐릭터 초상화와 장면 이미지처럼 역할이 다른 에셋은 자동 승계하지 않는다.
4. 읽을 수 없는 첨부와 출처가 불명확한 값은 `blocked` 또는 `null`로 남긴다. 대체 이미지 생성과 수치 추정은 하지 않는다.

## 근거 원장

| 대상 | 값·에셋 | 근거 | 허용 사용처 | 상태 |
|---|---|---|---|---|
| 아르고 | 기존 ACTIVE 사용자 계정 | 현재 DB 읽기 전용 확인 | 중복 계정 생성 방지 | 확인됨 |
| 아르고 / ACCEL | 엘 볼라도르 캐릭터 시트 | 2026-08-06 현재 사용자 제공 원문 | 캐릭터 등록 payload | 확인됨 |
| ACCEL | 세 역할별 원본 이미지와 로컬 누끼 | 2026-08-06 사용자 첨부 원본 3장, `hair` 작업의 4배경 시각 검수 | `previewImage`·`lore.mainImage`·`pixelCharacterImage` | 확인됨 |
| 네베드 CMMG 원본 | `cmmg-mk47-mutant-nosb-mod`, 피해 5/10/7, 탄창 6/6 | `docs/spec/equipment/cmmg-mk47-mutant-nosb-mod.md` | 원본 식별·변경 전 기준 | 확인됨 |
| 네베드 CMMG 개조 | 피해 7/12/12, 탄창 12/12 | 사용자 확정 대화 | 후보 스펙·테스트 payload | 확인됨 |
| 네베드 CMMG 개조 | 포스코어×1, 탄창 개조×1 | 사용자 확정 대화 및 기존 마스터 아이템 | 개인 재료 후보 | 확인됨 |
| 네베드 CMMG 개조 | 공임 크레딧 | 대화에는 크레딧 필요만 있고 금액이 없음 | 견적 | blocked |
| 깨진 음절 특수탄 | 공용 깨진 음절×3, 시즌 1 전체 3발, 거치 중 +30 방어 무시 심령 피해 | 사용자 확정 대화 | 승인 후 후보 액션 | 확인됨 |
| 네베드 개조 결과 이미지 | `/assets/catalog/equipment/cmmg-mk47-mutant-pian-bulwark.webp` | 사용자 승인 최종 거치대 강조 이미지 | 결과 장비 미리보기 후보 | 확인됨 |
| 깨진 음절 특수탄 이미지 | `/assets/catalog/consumables/zulu-0028-censor-3.webp` | 사용자 검토용 3발 세트 생성 결과 | 전용 소모품 미리보기 후보 | 확인됨 |
| 네베드 개조 결과 이름·설명 | `CMMG Mk.47 Mutant 「피안의 보루」` | 사용자 요청으로 생성한 명명·편집 제안 | 후보 spec | design-proposal / 승인 대기 |
| 깨진 음절 특수탄 이름·설명 | `ZULU-0028 파쇄음절탄 「CENSOR-3」` | 사용자 요청으로 생성한 명명·편집 제안 | 후보 spec | design-proposal / 승인 대기 |
| ZULU-0028 연구 | 격리 개체→최초 깨진 음절×1, 이후 500 CR당 ×1 | 사용자 확정 대화 및 기존 마스터 아이템 | `/erp/research` v1 | 확인됨 |
| ZULU-0028 격리 개체 이미지 | `/assets/catalog/special/zulu-0028-contained-entity.webp` | 저장소의 동일 slug 카탈로그 에셋 | 연구소 투입 시료 카드 | 확인됨 |
| 깨진 음절 이미지 | `/assets/catalog/samples/broken-syllable.webp` | 저장소의 동일 slug 카탈로그 에셋 | 연구소 추출 샘플 카드 | 확인됨 |
| 제노 | 신원·등급·소속·대사·초상화 | 정확한 설정과 역할별 에셋 미확보 | 연구소 NPC | blocked |
| 미스터비스트 소다 | 80% 10/10, 20% 1/1 | 사용자 확정 대화 | 서버 소모 판정 | 확인됨 |
| 문학·철학 | 신규 입력은 문학으로 통합 | 사용자 확정 대화 | 생성·수정 UI와 dry-run | 확인됨 |
| 문학·철학 충돌 | 합산·최댓값·덮어쓰기 규칙 | 확정 근거 없음 | 기존 중복 레코드 migration | blocked |
| 과거 캐릭터 프롬프트 첨부 | 여성 히어로 참고 이미지 | 이번 대상과 연결 근거 없음 | 이번 구현 전체 | 사용 금지 |

## 기능 결정

- 네베드 기본 개조와 깨진 음절 특수탄 제작은 별도 단계다. 특수탄 제작에는 관료 투표 동의가 필요하지만 투표 기간·통과 기준·동률·무투표 규칙은 아직 확정되지 않았다.
- 연구소 최초 제출은 GM 전용, 추가 추출은 MAIN 캐릭터 전용이다. 결과 재료는 공용 인벤토리에 들어간다.
- ZULU-0028의 기존 Sonic Emitter 연구 설정은 유지하고 깨진 음절 탄환을 이후 일회성 후보로 구분한다.
- Nochichim은 구조화된 ID·분류·슬롯·액션을 사용하고 이름·설명에서 장비 종류를 추정하지 않는다.
- 문학과 철학이 동시에 존재하는 레코드는 dry-run 충돌 목록에만 남기고 자동 변경하지 않는다.
- 네베드 후보를 실제 공방 프리셋으로 만들려면 공임, 제작 소요 시간, 담당 전문가, 액션 표시명과 안정 `U` 코드, 거치·회수 행동 비용, 특수탄의 일반 탄약 동시 소모 여부·정확한 사거리, 다른 피해 증가와의 결합 규칙, 투표 규칙과 실제 결과가 더 필요하다. 이 값들은 추정하지 않으므로 현재 구현에는 공용 재료 범위·복수 액션·거치·비재장전형 충전 계약만 추가한다.
- 복수 장비 액션의 충전은 액션 코드별로 분리한다. 현재 근거가 확인된 복수 충전 액션은 재장전 불가이므로, 별도 재장전 규격이 생기기 전까지 복수 액션 견적에서 재장전 가능 상태를 허용하지 않는다.
- 두 신규 카탈로그 문서는 승인 이미지를 연결한 `design-proposal`이며 master item이나 공방 blueprint 적재 입력이 아니다. `price: 0`, `isAvailable: false`, `isPublic: false`는 미확정 공임·판매·노출 정책을 대신하는 값이 아니라 오적재를 막기 위한 비공개·비판매 안전 센티널이다.

## 구현 상태

- `구현 완료`: `/erp/research` 화면과 GM 최초 제출·MAIN AGENT 추가 추출 API, 정확한 두 WebP 에셋 연결, 멱등 경제 operation과 transaction 내 재검증.
- `구현 완료`: 미스터비스트 소다 단위별 서버 판정(20% HP/SAN 1, 80% HP/SAN 10), ERP·Nochichim 소비 결과 replay, Nochichim 실제 회복의 요청별 1회 적용, commit 후 알림과 캐시 갱신.
- `구현 완료`: 신규·수정 캐릭터의 정확한 `철학` 표기를 `문학`으로 통합하고, 기존 값을 건드리지 않는 dry-run migration과 이중 실행 가드.
- `구현 완료`: 공방 재료의 개인/공용 범위, 복수 `U` 액션, 코드별 충전, 일반 탄약·거치 프로필, 구조화된 Nochichim snapshot과 멱등 충전 소비.
- `구현 완료`: 충전형 장비 액션의 충전과 일반 탄약을 MongoDB 한 번의 조건부 갱신으로 차감하고, 멱등 replay 때 저장 당시 snapshot이 아닌 최신 장비 상태를 반환.
- `구현 완료`: Nochichim 자동 로그인 동기화에서 현재 HP/SAN은 보존하고 최대치·ATK·DEF·장비 계약만 갱신. 거치 상태는 세션 로컬이며 거치 중 플레이어 이동을 차단. 거치/회수 행동 비용, 거치 피해 보너스, 거치 중 대각선 사격 제한도 구조화 필드로만 판정.
- `구현 완료`: Nochichim 구조화 장비는 API가 전달한 명시적 `U` 액션만 노출한다. 정확한 격자 사거리가 없는 공격은 사용 불가로 닫고, `W` 공격·`R` 재장전·0~4칸 사거리·장비명 기반 분류를 새 계약에 자동 생성하지 않는다.
- `구현 완료`: Nochichim 장비 외부 차감은 요청 ID와 `PENDING → COMMITTED → LOCAL_APPLIED → APPLIED` 원장을 캐릭터 저장소에 남긴다. `LOCAL_APPLIED`에는 전장 적용 전후 snapshot과 메시지를 저장한 뒤 방·토큰·턴·대화 이력을 확정하며, 응답 유실·프로세스 재시작 뒤에는 브라우저 sessionStorage 또는 동일 fingerprint의 기존 원장 ID를 재사용한다.
- `구현 완료`: 캐릭터별 외부 장비·소모품 mutation은 처리 중 중복 실행을 차단한다. 활성 소모품 복구는 저장된 과거 인벤토리 배열을 적용하지 않고 같은 멱등 요청을 ERP에 재전송해 최신 snapshot을 받으며, 시트 조회 revision은 진행 중이거나 더 최신인 mutation 결과를 덮어쓰지 못한다.
- `구현 완료`: 구조화 `CHARGED` 액션에 기계 판정 규격이 없으면 설명을 추정해 실행하지 않고 `unsupported-effect`로 닫는다. 현재 확인된 `U3`·`U5`도 같은 fail-closed 계약으로 검증한다.
- `구현 완료`: ERP·VTT 소모품 transaction은 ACTIVE 사용자, MAIN AGENT 또는 기존 GM 소유 NPC, 소유자, 마스터 아이템 ID·slug·분류를 transaction 안에서 다시 확인한다. 응답과 알림은 사전 조회값이 아니라 commit된 소유자·코드네임·품목명을 사용한다.
- `구현 완료`: Nochichim 작전 크레딧 스킬 비용·실패 보상·undo는 요청 ID가 있는 원자적 `adjust` operation을 사용한다. 같은 요청 replay는 한 번만 반영하고, 다른 writer의 증감을 절대값 복원으로 덮어쓰지 않는다. GM의 명시적 절대값 변경만 atomic set으로 분리한다.
- `구현 완료`: 크레딧 스킬 실패 보상은 외부 보상 호출 전에 `ROLLBACK_PENDING`과 전장 snapshot을 로컬 원장에 먼저 저장한다. 저장 실패 시 보상을 호출하지 않으며, 응답 유실·프로세스 재시작 뒤 동일 보상 ID로 재개한다. 외부 응답 뒤 전장 상태를 다시 비교해 달라졌으면 과거 snapshot을 복원하지 않고 수동 정산 상태로 닫는다.
- `구현 완료`: Nochichim 캐릭터 응답에서는 장비·소모품·크레딧 스킬 내부 operation receipt를 제거한다. 완료·실패한 스킬 receipt도 전장 복구 snapshot을 폐기해 소유자·GM 응답과 archive/delta에 내부 상태가 남지 않는다.
- `구현 완료`: 공방 운영 편집기는 기존 견적의 복수 장비 액션과 전투 프로필을 편집본에 보존하며, 상태를 가진 장비 stack은 수량 1이 아니면 escrow 전에 중단한다.
- `시트·자산 준비 완료 / payload 차단`: ACCEL 시트·Argo ACTIVE 계정·세 역할별 WebP 자산과 codename resolver 연결을 확인했다. `J`, 잔여 포인트 0은 확정됐지만 `MAIN / 공개 / UNASSIGNED`가 미확정이므로 실행 가능한 payload와 라이브 생성·전기톱 지급은 만들거나 수행하지 않았다.
- `이미지 완료 / 이름·설명 후보`: `피안의 보루`와 `CENSOR-3`의 이미지 자산은 저장소에 준비했다. 이름과 편집 설명은 `design-proposal`로 구조화 검증만 했으며 사용자 승인 전에는 catalog·공방에 적재하지 않는다.
- `보류`: 제노 NPC와 네베드 실행 가능한 공방 프리셋. 동일 대상의 정확한 근거가 없거나 필수 결정값이 남아 있어 생성하지 않았다.
- `미실행`: 라이브 연구 제출·추출, 공방 견적/수락/납품, 소다 소비, 캐릭터 migration, Discord 투표·알림·배포.

## 읽기 전용 점검 결과

- 문학·철학 migration dry-run 후보 26건 중 자동 변경 후보 2건, 변경 불필요 23건, 수동 판단 1건이었다. 실제 DB 쓰기는 실행하지 않았다.
- 아르고는 기존 ACTIVE 사용자 계정이며 현재 소유 캐릭터가 0명이고 `ACCEL` 중복도 없다. 네베드는 원본 CMMG 1개를 무기 슬롯에 장착 중이며 개인 포스코어·탄창 개조는 0개, 공용 깨진 음절은 정확히 3개다. 라이브 캐릭터·인벤토리 mutation은 실행하지 않았다.

## 검증 게이트

- 경제·인벤토리 mutation은 테스트 DB 또는 mock에서 멱등성, 중복 요청, 동시성, 실패 복구를 검증한다.
- 변경 패키지의 집중 테스트, `pnpm typecheck`, `pnpm lint`, 필요 시 `pnpm build`를 통과한다.
- 사용자 화면은 로컬 인증 환경에서 데스크톱·모바일, 빈 이미지 상태, 콘솔 오류와 가로 넘침을 확인한다.
- 라이브 실행은 정확한 대상과 변경 전후 상태를 제시하고 별도 승인을 받은 뒤 수행한다.

## 구현 검증 결과

- StarGateV2 `pnpm typecheck`, `pnpm lint`, `pnpm build` 통과. 프로덕션 빌드에는 공유 DB와 core 패키지 빌드가 포함된다.
- 집중 계약 테스트 75건 중 69건 통과, MongoDB 테스트 URI가 필요한 6건은 명시적으로 skip, 실패 0건. 추가 경제 operation 통합 테스트 9건도 같은 환경 변수 부재로 명시적으로 skip했다. 클레이모어 프리셋은 저장소의 정확한 `.webp` 에셋 계약으로 통과한다.
- Nochichim 장비 룰, 구조화 시트, 로그인 HP/SAN 보존, API 재시도, 장비·소모품·크레딧 스킬 원장, 소다 회복, 작전 크레딧 멱등 증감·응답 유실·rollback/undo, 내부 receipt 비노출, Susan 장비 판정 관련 테스트 파일 17개를 모두 통과했고 관련 JavaScript 파일 10개의 `node --check`를 통과했다. 지연된 보상 응답 중 전장 상태가 바뀐 테스트는 과거 snapshot 복원과 방 저장이 모두 0회임을 확인한다.
- Nochichim 전체 383개 테스트도 실행했다. 첫 실행에서 이번 장비 상태 추가와 맞닿은 DND 기본 snapshot 기대값을 갱신했고, 재실행에서는 26건이 실패했다. 그중 새 소모품 원장 구조와 직접 맞닿은 DM 가시성·백장미 승인 계약은 갱신 후 단독 통과했다. 나머지는 이번 범위 밖에서 함께 존재하는 대규모 미커밋 UI/런타임 변경의 정적 marker 불일치와, 이번에 수정하지 않은 이동 경로 E2E timeout 등으로 전체 green 근거에는 포함하지 않는다.
- 로컬 인증 브라우저에서 `/erp/research`는 정확한 ZULU-0028·깨진 음절 WebP만 표시하며 데스크톱·390×844에서 가로 넘침과 콘솔 오류가 없음을 확인했다. 제출·추출 버튼은 누르지 않았다.
- `/erp/admin/equipment-workshop`은 인증 상태로 200 응답, 데스크톱 가로 넘침 없음, 요청·결과 이미지 역할 분리와 장비 액션 편집 영역의 레이아웃을 확인했다. 검토·견적·수락·납품 등 mutation은 실행하지 않았다.

## 관련 구현 커밋

- StarGateV2: `30ffe959` (`feat(novusweb): 시즌 1 운영 연동을 확장한다`)
- Nochichim: `9ddb539` (`feat: StarGate 전투 연동 원장을 보강한다`)
