import wikiCategories from "@/docs/spec/wiki-categories.json" with { type: "json" };

/** Controlled wiki taxonomy and shared presentation order. */
export const WIKI_CATEGORY_ORDER: readonly string[] = wikiCategories;
